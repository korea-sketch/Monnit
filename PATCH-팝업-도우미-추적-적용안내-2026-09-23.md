# 팝업 · 배너 교체 · 유입/이동 추적 — 적용 안내 (2026-09-23)

## 적용
압축 안의 `Monnit-main/` 내용을 저장소 루트에 덮어쓰고 커밋·푸시 (새 파일 추가 + 기존 파일 수정만, 삭제 없음)
```bash
rsync -a Monnit-main/ /경로/내-저장소/
git add -A && git commit -m "feat: 사이트 팝업 + 설치사례 배너(문구 없는 버전) + 유입·경로 추적(/ops/flow)" && git push
```
추가 환경변수 없음. (선택) 이제 안 쓰는 `images/field-review/hero-v2-*`, `hero-m-*` 는 지워도 됩니다.

## 1) 설치 사례 배너
- /installation-photos 상단 배너 → 문구 없는 버전(v3). 모바일은 봉황 중심 2:1 크롭(m3), 배너 위 HTML 문구도 제거
- 파일명을 새로 바꿔서 30일 이미지 캐시에 걸리지 않음

## 2) 사이트 팝업 — 맞춤형 제안서 무료 제작 EVENT (로봇)
- 파일: `js/monnit-popup.js` + `images/popup/robot-body|wing-l|wing-r.webp` (로봇을 몸통·양 날개로 분리)
- 문구는 전부 HTML 텍스트라 어느 화면에서도 선명함
- 효과: 로봇 낙하 등장 → 둥실 떠다님 · 날갯짓 · 안테나 불빛 · 눈 깜빡임 / 반짝이 별 · 뱃지 광택 · 강조 문구 흐르는 빛 · 버튼 광택+파동 · 배경 원 움직임 · 떠오르는 입자 / 문구 순서대로 등장
  (기기에서 '동작 줄이기'를 켠 사용자는 효과 없이 정지 화면)
- 크기: 폭 최대 400px, 화면이 좁거나 낮으면 비율 유지하며 자동 축소 (360×640 에서도 한 화면)
- 카드 어디를 눌러도 → `/proposal?from=finder&fac=public&con=air&fl=공공·교육·문화&cl=공기질·환경`
- **켜기/끄기: /ops/flow 상단 스위치** (재배포 불필요, 1분 안에 반영)
- 탭당 1회 · 「오늘 하루 보지 않기」(한국시간 자정까지) · X / 닫기 / 배경 / ESC
- 띄우지 않는 곳: /proposal, /visit, /contact, /promo/* 광고 랜딩, 관리·개인정보 화면
- 팝업 id 가 바뀌어(proposal-event-2026-09) 예전에 「오늘 그만 보기」 누른 사람에게도 새 팝업은 뜸

## 3) 유입·이동 추적 — /ops/flow (관제 메뉴 「유입·경로」)
- 파일: `js/monnit-track.js` → `/api/track` (`netlify/functions/track.mjs`) → Blobs `track` 저장소(월별)
- 보는 것: 유입 채널 · 외부 유입 사이트 · 첫 페이지×채널 · 페이지별(보기/세션/진입/이탈) · 페이지 상세(어디서 왔나 / 어디로 갔나 / 누른 것) · 페이지 이동 Top · 클릭 Top · 최근 방문 흐름 · 팝업 노출/클릭률
- SPA 화면 이동(pushState)도 페이지 보기로 기록
- 개인정보: IP·쿠키·입력값 저장 없음, 탭 단위 난수만. 관제 로그인 브라우저는 「내부」로 기본 제외
- 조각 파일은 proposal-dispatch(10분마다)가 합침. 엣지 함수는 /api/track·/api/popup 을 건너뜀

## 4) 상시 도우미 로봇 (2026-09-23 추가)
- 파일: `js/monnit-pet.js` (전 페이지 포함, 로봇 이미지는 팝업과 같은 파일을 씀)
- 화면 오른쪽 아래에 늘 떠 있음 — 들어오자마자 보이고, 1.3초 뒤 말풍선으로 한 줄 안내
- 로봇 누르면 안내 카드: 「단 1분 만에 제안서 받아보기」 / 상담 문의 / 전화 02-2088-1454
- 하단 고정 바(모바일)·상담 버튼(PC)·쿠키 안내와 겹치지 않게 자동으로 그 위로 올라감
- 사진 확대·팝업 같은 전체 화면 창이 열리면 잠시 숨음 / 모바일 스크롤 중엔 살짝 작아짐
- 「도우미 숨기기」 → 가장자리 작은 손잡이로 접힘(그 탭 동안), 누르면 다시 펼침
- 켜기/끄기: /ops/flow 「상시 도우미」 스위치 · 클릭 통계도 같은 곳에
- 안 나오는 곳: /proposal, /visit, /contact, /promo/* 광고 랜딩, 관리 화면

## 5) 설치 사례 페이지 — 캡처 방지 가림막 제거
- 창 전환·탭 이동 때마다 화면을 덮던 「PROTECTED CONTENT」 가림막과 단축키 차단(F12·Ctrl+S 등) 제거
- 사진을 끌어서 저장·길게 눌러 저장·우클릭 저장만 계속 막음 (사진에서만, 다른 곳 우클릭은 정상)

## 6) 설치 사례 페이지 — 홈·메뉴 개선
- PC: 상단에 사이트 메뉴(회사 소개·제품·활용 분야·도입 사례·프로모션·맞춤 제안서) + 테두리 있는 「홈」 버튼
- 모바일: 「홈」 버튼 + ☰ 전체 메뉴(홈으로·각 메뉴·맞춤 제안서·상담·전화)
- 로고 클릭 → 홈, 상단 아래에 현재 위치 표시(홈 › 도입 사례 › 설치 사례 갤러리)
