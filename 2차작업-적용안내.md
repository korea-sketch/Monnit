# 2차 작업 적용 안내 (2026-07-27)

앞서 올린 프로모션 랜딩 위에 **6가지 기능**을 추가했습니다.

---

## 요약

| # | 요청 | 결과 |
|---|---|---|
| 1 | 컨설팅 프로모션을 8/1 KST에 딱 열기 | 시작 전에는 카드 클릭 시 「아직 시작 전」 안내창, 8/1 00:00부터 자동 오픈 |
| 2 | 7/30 지난 프로모션 진입 차단 | 카드 흐림+`종료` 도장, 클릭·직접 URL 모두 차단하고 진행 중 혜택으로 유도 |
| 3 | 진행 건수 카운터 정확히 | 진행 중인 것만 계산 → 지금은 **6건**, 8/1부터 **1건** |
| 4 | 컨설팅 대표 이미지 교체 | 보내주신 배너로 교체 (카드 썸네일 + 카톡 공유 썸네일) |
| 5 | 쿠키 수집 안내 | 첫 방문 시 동의 배너 + 개별 설정 창, 동의 전 광고·분석 쿠키 미저장 |
| 6 | church 페이지 QR 전용 | `/church` 배포, QR 코드로만 입장, 이탈 시 재입장 불가, 검색 차단, 리드 수집 연동 |

---

## 1. 프로모션 기간 자동 관리 ⭐

### 어떻게 판단하나

**한국 시간(KST) 기준**으로 계산합니다. 방문자가 해외에 있어도, 기기 시계가 틀려도 동일하게 동작합니다.

날짜는 두 곳에서 읽습니다.

1. `Promotions` 시트의 **`start` / `end` 열** (있으면 최우선)
2. 없으면 **`period` 문구를 자동 해석**

자동 해석되는 표기:

| 시트 `period` 값 | 해석 결과 |
|---|---|
| `7/30까지` · `7/30 일까지` | ~ 7월 30일 24:00 |
| `8월 한정` | 8월 1일 00:00 ~ 8월 31일 24:00 |
| `8/1 ~ 8/31` | 8월 1일 00:00 ~ 8월 31일 24:00 |
| `2026.08.01 ~ 08.31` | 위와 동일 |
| `~ 9/15` | ~ 9월 15일 24:00 |

> 연도를 안 쓰면 올해로 보되, 6개월 이상 과거로 계산되면 내년으로 간주합니다. (연말·연초 대응)

### 상태별 동작

| 상태 | 카드 모습 | 클릭하면 |
|---|---|---|
| **진행 중** | 정상 (배지 표시) | 상세 팝업 또는 랜딩페이지로 이동 |
| **시작 전** | 흐림 + 파란 `시작 전` 도장, 「N월 N일 오픈 예정」 | 「아직 시작 전입니다 · N월 N일부터 신청 가능」 안내창 |
| **종료** | 흑백 + 회색 `종료` 도장, 「N월 N일 종료」 | 「종료된 프로모션입니다」 + **진행 중인 혜택으로 유도** |

- 주소창에 `#promotions/fire` 를 직접 쳐도 동일하게 차단되고 안내창이 뜹니다.
- 정렬 순서: 진행 중 → 시작 전 → 종료
- 사전신청 폼의 프로모션 드롭다운에도 **진행 중인 것만** 나옵니다.

### 날짜를 바꾸고 싶을 때

**컨설팅 프로모션**은 `app.js` 안 `BUILTIN_PROMOS` 에서 수정합니다.

```js
start: '2026-08-01',   // 이 날 00:00 KST 부터 오픈
end:   '2026-08-31',   // 이 날 24:00 KST 까지
```

**시트에 있는 프로모션**은 `period` 문구만 고치면 됩니다. 더 정확히 하려면 `Promotions` 탭 헤더 끝에 `start`, `end` 열을 추가하고 `2026-08-01` 형식으로 넣으세요.

**기간과 무관하게 즉시 내리려면** 시트 `ended` 열에 `1` 을 넣습니다.

---

## 2. 쿠키 동의

### 어디에 뜨나

메인 사이트, 프로모션 랜딩(`promo.html`, `promo-consulting.html`), `/church` 전부에 적용됩니다.

### 어떻게 동작하나

1. 첫 방문 → 화면 하단에 안내 배너가 올라옵니다
   - 제목: **「귀하의 개인정보를 소중히 여깁니다」**
   - 링크: `쿠키 공지` (개인정보처리방침) · `환경설정 관리` (상세 설정 창)
   - 버튼: **`쿠키 거부`** / **`모든 쿠키 수락`**
2. 선택 결과를 브라우저에 저장 → 다음 방문부터는 배너가 뜨지 않음
3. 푸터의 **`쿠키 설정`** 링크 또는 배너의 `환경설정 관리` 로 언제든 변경·철회 가능
   - 상세 창에서는 `필수` / `분석` / `광고·마케팅` 을 개별 토글

### 무엇을 차단하나

**Google 동의 모드(Consent Mode v2)** 를 적용했습니다.

| 도구 | 동의 전 | 동의 후 |
|---|---|---|
| Google Tag Manager · GA4 | 로드는 하되 **쿠키 저장 안 함** (익명 신호만) | 정상 동작 |
| Google Ads 전환·리마케팅 | 차단 | 정상 동작 |
| Microsoft Clarity | **아예 로드 안 함** | 로드 |

> 이 방식은 개인정보보호법·GDPR의 옵트인 원칙을 지키면서도, Google이 제공하는 **전환 모델링**으로 광고 성과 측정 손실을 최소화합니다. 태그를 완전히 막으면 거부한 방문자의 데이터가 통째로 사라집니다.

### 문구·색상 바꾸기

`js/monnit-consent.js` 파일 안에서 수정합니다.

- 배너 문구 → `function banner()`
- 설정 창 문구 → `function settings()`
- 버튼 색 → 상단 `CSS` 배열의 `#2f7d32`

### 개인정보처리방침

`/privacy.html` 을 새로 만들었습니다. 쿠키·국외이전·보유기간 등 관련 조항이 들어 있고, 푸터와 동의 창에서 링크됩니다. **게시 전 사내 검토를 권장합니다.**

---

## 3. church 페이지 (전단지 QR 전용)

### 주소와 QR

배포 주소: **`https://monnit.co.kr/church`**

전단지 QR에는 **코드를 붙인 주소**를 넣어야 합니다.

```
https://monnit.co.kr/church/?k=MNK5230
```

> `?k=MNK5230` 이 없으면 입장이 차단됩니다. UTM을 붙이려면 뒤에 이어서 쓰면 됩니다.
> 예) `https://monnit.co.kr/church/?k=MNK5230&utm_source=flyer&utm_campaign=church_2026q3`

### 접근 규칙

| 상황 | 결과 |
|---|---|
| QR로 입장 (`?k=` 있음) | ✅ 정상 열람 · 주소창에서 코드는 자동 제거 |
| 열람 중 새로고침 | ✅ 계속 열람 가능 |
| 전화·개인정보방침 등으로 잠깐 이탈 | ✅ 2분 이내 복귀 시 계속 열람 |
| 탭 닫기 / 다른 사이트로 이동 / 뒤로가기 | 🔒 **이후 재입장 불가** |
| 이탈 후 같은 브라우저로 재접속 | 🔒 「열람이 종료되었습니다」 화면 |
| QR 없이 주소만 입력 | 🔒 「전단지 QR로만 열람할 수 있습니다」 화면 |
| 구글 검색 | 🔒 색인 차단 (noindex + robots.txt + X-Robots-Tag) |

- 차단 화면에서도 **전화번호는 크게 노출**되어 상담은 계속 받을 수 있습니다.
- 페이지 상단에 「이 화면을 벗어나면 다시 열 수 없습니다」 안내 띠가 항상 표시됩니다.
- 신청서를 작성하다 나가려 하면 브라우저 확인창이 뜹니다.

> ⚠️ 차단 기준은 **브라우저 단위**입니다. 다른 기기·시크릿창으로는 재접속됩니다. 서버 없이 할 수 있는 최선이며, 완전한 1인 1회 차단은 별도 서버가 필요합니다.

### 설정 바꾸기

`church/index.html` 상단 `CFG` 에서 조정합니다.

```js
keys: ['MNK5230','CHURCH2026'],  // QR 코드 (여러 개 가능)
graceSec: 120,                   // 잠깐 이탈 허용 시간(초)
devBypass: 'MNKPREVIEW',         // 내부 확인용 (?k=MNKPREVIEW · 이탈해도 안 잠김)
allowRescan: false               // true 로 바꾸면 이탈 후 QR 재스캔 시 다시 열람 가능
```

> 💡 **내부 확인은 `?k=MNKPREVIEW` 로 하세요.** 일반 코드로 확인하면 본인 브라우저가 잠겨버립니다.
> 실수로 잠겼다면 브라우저 개발자도구 콘솔에서 `localStorage.removeItem('mnk_church_pass_v1')` 후 새로고침하면 풀립니다.

### 신청서 연동

기존에는 메일 앱만 열렸는데, 홈페이지와 **동일한 접수 체인**으로 바꿨습니다.

```
StaticForms → 실패 시 Web3Forms → 실패 시 메일앱
```

접수 항목: `신청 프로모션 / 교회명·성함 / 전화번호 / 교회 규모 / 프로모션 코드 / 동의 2종 / 동의 일시 / 접수 경로 / 출처 / 유입 페이지`

QR 스캔 유입도 GA4에 `generate_lead` 로 집계됩니다.

### 기존 페이지는 그대로

`https://monnit.co.kr/#promotions/church` (사이트 안의 교회 프로모션 팝업)는 **손대지 않았습니다.** 다만 기간이 `7/30까지` 이므로 7/31부터는 자동으로 종료 처리됩니다.

---

## 4. 변경·추가된 파일

### 새 파일

| 파일 | 설명 |
|---|---|
| `js/monnit-consent.js` | 쿠키 동의 (전 페이지 공통) |
| `privacy.html` | 개인정보처리방침 |
| `church/` (10개 파일) | 전단지 QR 전용 랜딩 + 이미지 |
| `images/promo-consulting-banner.jpg` · `.webp` | 컨설팅 대표 이미지 |
| `2차작업-적용안내.md` | 이 문서 |

### 수정된 파일

| 파일 | 내용 |
|---|---|
| `app.js` | 기간 스케줄링, 상태별 카드, 안내창, 카운터 |
| `style.css` | 종료·시작전 카드 스타일, 안내창 스타일 |
| `index.html` | 쿠키 동의 적용, 푸터 링크 |
| `promo.html` | 쿠키 동의 적용, 푸터 링크 |
| `promo-consulting.html` | 쿠키 동의, 대표 이미지, 오픈 예정 안내 |
| `_redirects` · `netlify.toml` · `_headers` | `/church` 라우팅 + 색인 차단 |
| `robots.txt` · `build.js` | `/church` 크롤링 차단, 사이트맵 |
| `sitemap.xml` | `privacy.html` 추가 |

---

## 5. 올리는 방법

지난번과 동일합니다.

```
cp -R /압축푼경로/Monnit-main/. ~/Documents/GitHub/Monnit/
```

GitHub Desktop → 파일 체크 → `Summary` 입력 → `Commit to main` → `Push origin`

---

## 6. 배포 후 확인

- [ ] `monnit.co.kr/#promotions` — 카운터가 **「현재 6건」** 인가
- [ ] 컨설팅 카드에 **`시작 전`** 도장과 「2026년 8월 1일 오픈 예정」이 보이는가
- [ ] 컨설팅 카드 클릭 → 「아직 시작 전인 프로모션입니다」 안내창이 뜨는가
- [ ] 첫 방문(시크릿창)에서 **쿠키 동의 배너**가 뜨는가
- [ ] 푸터 **`쿠키 설정`** 클릭 → 설정 창이 열리는가
- [ ] `monnit.co.kr/church` 접속 → **「전단지 QR로만 열람」** 차단 화면
- [ ] `monnit.co.kr/church/?k=MNK5230` 접속 → 정상 열람 + 상단 안내 띠
- [ ] 위 상태에서 다른 사이트로 이동했다가 다시 `/church` → **차단 화면**
- [ ] (내부 테스트는 `?k=MNKPREVIEW` 사용)

### 8월 1일 오픈 당일 확인

- [ ] 00:00 이후 컨설팅 카드가 정상 색으로 바뀌고 `NEW` 배지가 붙는가
- [ ] 클릭하면 `/promo/consulting` 으로 이동하는가
- [ ] 카운터가 **「현재 1건」** 으로 바뀌었는가
- [ ] 기존 6개 카드가 흑백 `종료` 처리되었는가

> 미리 확인하고 싶으시면 컴퓨터 시계를 8월 1일로 바꾸고 새로고침해 보세요. (KST 기준으로 동작)

---

## 7. 검증 내역

브라우저 환경(jsdom)에서 실제로 실행해 확인했습니다.

| 항목 | 결과 |
|---|---|
| 기간 문구 6종 파싱 | 전부 정확 |
| 7/27 · 7/30 23:59 · 7/31 · 8/1 00:00 · 9/1 시점별 상태 | 전부 의도대로 |
| 안내창 4가지 경우(시작 전 / 종료+대체있음 / 종료+대체없음) | 문구·유도 링크 정상 |
| 쿠키 동의 5가지 경우(첫방문 / 필수만 / 전체 / 개별 / 재방문) | 동의값·태그 로드 정확 |
| church 게이트 6가지 경우 | 차단·허용 판정 정확, 본문 완전 제거 확인 |
| 폼 전송 payload | 12개 항목 + UTM 출처 정상 |
| JS 구문 검사 | 오류 0건 |


---

## 8. 실제 연동 점검 결과 (2026-07-27 확인)

### 이메일 접수 — ✅ 정상

`https://api.staticforms.dev/submit` 로 실제 전송해 확인했습니다.

```
{"success":true,"message":"Form submitted successfully","id":"f8b7b5e4-…"}
HTTP 200
```

> `korea@monnit.com` 메일함에 **「[테스트] 연동 점검 - 무시하세요」** 제목의 메일이 도착했는지 확인해 주세요.
> 도착했다면 실제 신청서도 동일하게 들어옵니다.

**2순위 Web3Forms** 는 서버에서 호출하면 403(`Use our API in client side`)을 돌려줍니다.
이는 무료 플랜이 서버 요청만 막는 것으로, **방문자 브라우저에서 호출될 때는 정상 동작**합니다.
키 자체가 무효한 것이 아니므로 폴백 경로는 살아 있습니다.

### 스프레드시트 연동 — ⚠️ 탭을 만들어야 작동

`PromoConsulting` 탭이 **아직 없습니다.**

확인 과정에서 중요한 사실을 발견했습니다 — 구글 gviz는 **존재하지 않는 탭 이름을 요청하면 오류 대신 "첫 번째 탭"의 내용을 돌려줍니다.**
지금 요청하면 README 안내문이 돌아옵니다. 그대로 두면 엉뚱한 내용이 랜딩페이지를 덮어쓸 위험이 있어, **이 페이지 전용 키가 3개 이상 확인될 때만 반영하도록 안전장치를 추가**했습니다.

→ 4장 순서대로 `PromoConsulting` 탭을 만들고 CSV를 붙여넣으시면 즉시 작동합니다.

### `Promotions` 탭 — 손대지 않아도 됨

`link` 열도, `consulting` 행도 없지만 **app.js 내장 프로모션으로 표시**되므로 문제 없습니다.
나중에 시트에서 관리하고 싶으면 `link` 열을 추가하고 `consulting` 행을 넣으면 시트 값이 우선 적용됩니다.

### 배포 상태 — ❌ 2차 작업분 미반영

확인 시점 기준 서버 상태입니다.

| 경로 | 상태 |
|---|---|
| `/promo/consulting` | ✅ 200 (1차 작업분) |
| `/app.js` 기간 스케줄링 | ❌ 미반영 |
| `/js/monnit-consent.js` | ❌ 404 |
| `/privacy.html` | ❌ 404 |
| `/church/` | ❌ 404 |
| `/images/promo-consulting-banner.jpg` | ❌ 404 |

→ 이번 zip을 올리고 커밋·푸시해야 반영됩니다.

### 검증 중 발견해 고친 버그 3건

| 버그 | 증상 | 조치 |
|---|---|---|
| 시트 오인식 | 탭이 없을 때 README 내용이 페이지를 덮어쓸 수 있었음 | 전용 키 확인 후에만 반영 |
| 이벤트바 덮어쓰기 | 오픈 예정 안내가 `bar-01` 키를 지워 시트 수정이 안 먹힘 | `data-k` 유지하도록 수정 |
| church UTM 유실 | QR 코드 제거하면서 `utm_*` 까지 같이 지워짐 | `k` 파라미터만 제거하도록 수정 |

추가로 church 신청서의 값 읽기 방식을 `form.<name>` → `getElementById` 로 바꿔 모든 브라우저에서 안전하게 동작하도록 했습니다.

---

## 9. 데이터 추적 — 무엇이 자동이고 무엇을 직접 하셔야 하나

### 코드에서 이미 처리된 것 ✅

모든 이벤트를 `gtag()` 로 **GA4에 직접** 보냅니다. GTM 안에 태그·트리거를 따로 만들지 않아도 수집됩니다.

| 이벤트 | 언제 | 전송처 |
|---|---|---|
| `generate_lead` | 신청서 제출 (컨설팅 랜딩 · 교회 랜딩) | GA4 · GTM · Clarity |
| `call_click` | 전화 버튼 클릭 | GA4 · GTM · Clarity |
| `cta_click` | 주요 CTA 클릭 | GA4 |
| `promo_card_click` | 프로모션 카드 클릭 | GA4 |
| `promo_gate_view` / `promo_gate_redirect` | 종료·시작전 안내창 노출·이동 | GA4 |
| `page_view` | 해시 라우팅 페이지 이동 | GA4 |
| `cookie_consent_update` | 쿠키 동의 변경 | GTM |

동의 모드(Consent Mode v2)도 코드에서 자동 처리되므로 GA4·Ads 쪽 추가 설정이 필요 없습니다.

### 직접 하셔야 하는 것 ❌ (계정 접근 권한이 없어 대신 못 합니다)

**① GA4에서 `generate_lead` 를 주요 이벤트로 표시 — 필수**

`generate_lead` 는 자동으로 주요 이벤트가 되지 않습니다. 직접 켜야 합니다.

```
GA4 → 관리 → 데이터 표시 → 이벤트
→ generate_lead 오른쪽 [주요 이벤트로 표시] 토글 ON
```

> 배포 후 이벤트가 최소 1회 발생해야 목록에 나타납니다. 신청서를 한 번 테스트 제출한 뒤 24시간 내에 확인하세요.
> 토글을 켜도 반영에 수 분~수 시간 걸립니다.

**② Google Ads 전환 가져오기 — 광고를 돌리신다면 필수**

```
Google Ads → 목표 → 전환 → 새 전환 작업
→ 가져오기 → Google 애널리틱스 4 속성 → generate_lead 선택
```

이걸 해야 광고비가 "전화 문의를 만든 클릭" 쪽으로 최적화됩니다. 안 하면 데이터만 쌓이고 입찰에는 반영되지 않습니다.

**③ Clarity 프로젝트 ID 정리 — 권장**

현재 두 개로 갈려 있습니다.

| 페이지 | Clarity ID |
|---|---|
| 메인 사이트 (`index.html`, `privacy.html`) | `xikw9t2skq` |
| 프로모션·교회 랜딩 | `x38egtft64` |

기존 설정을 그대로 유지한 것입니다. 한 프로젝트에서 모아 보시려면 각 HTML의 `clarityId:` 값을 하나로 통일하세요.

**④ GTM 컨테이너 안에 GA4 설정 태그가 있는지 확인 — 권장**

컨테이너(`GTM-T8H73VW`) 안에 `G-49THHRYKR4` 를 쏘는 GA4 구성 태그가 이미 있다면, 코드의 `gtag('config')` 와 겹쳐 **페이지뷰가 두 번 집계**될 수 있습니다.

```
GTM → 태그 목록에서 "GA4 구성" 또는 "Google 태그" 확인
→ 있으면 둘 중 하나만 남기기 (코드 쪽을 남기는 편이 단순합니다)
```

> 이 구조는 이번 작업 이전부터 동일했으므로, 지금까지 숫자가 정상이었다면 그대로 두셔도 됩니다.

**⑤ GTM에 Meta·네이버 등 다른 픽셀이 있다면 — 해당 시**

GA4·Google Ads 태그는 동의 모드를 자동으로 따르지만, **직접 만든 커스텀 태그는 따르지 않습니다.**
그런 태그가 있다면 GTM에서 각 태그의 `동의 설정 → 추가 동의 확인` 에 `ad_storage` / `analytics_storage` 를 지정해 주세요.

### 배포 후 확인 방법

```
GA4 → 보고서 → 실시간
```
에서 랜딩페이지를 열고 전화 버튼을 눌러 `call_click`, 신청서를 제출해 `generate_lead` 가 찍히는지 보시면 됩니다.
쿠키 배너에서 **거부**를 누른 상태에서는 이벤트는 오되 쿠키가 저장되지 않는 것이 정상입니다.
