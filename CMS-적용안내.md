# CMS 기능 추가 — 적용 안내 (2026-07-29 · v104)

## 이번에 바뀐 파일 (7개)

| 파일 | 바뀐 내용 |
|---|---|
| `index.html` | 파인더 Q1·Q2 를 `finder.config` 로 그리도록 전환 · 에셋 버전 v104 |
| `app.js` | 준비 중 화면 추가 · 파인더 갱신 훅 · 에셋 버전 v104 |
| `editor.html` | 🧭 파인더 항목 패널 신규 · 사진 드래그 순서 · 캡션 유실 버그 수정 · 순서 드래그 버그 수정 |
| `church/index.html` | 에셋 버전 v104 |
| `promo.html` · `privacy.html` · `promo-consulting.html` | 에셋 버전 v104 |

> Apps Script(`Code.gs`) 는 깃헙이 아니라 **구글 시트 쪽에 붙여넣는 코드**라 이 저장소에 없습니다.
> 따로 받은 `Code.gs` 를 Apps Script 편집기에 붙여넣고 "새 버전"으로 배포하세요.

---

## 새 Q1·Q2 구조가 바로 적용됩니다

`index.html` 에 새 분류를 **기본값으로 넣어 두었습니다.** 커밋·배포하면 시트 작업 없이 바로 보입니다.

**Q1 — 13개 → 11개**

| 변경 | 내용 |
|---|---|
| 통합 | 빌딩·오피스 + 리테일·매장·외식 → **상업용 건물** |
| 신규 | **소형 건물 · 중형 건물 · 대형 건물** |
| 삭제 | 주거·아파트, 호텔·리조트, 학교·공공기관, 그 외 시설 |

**Q2 — 10개 그대로.** 대신 `전력·에너지 절감` 의 연결 대상이 시설에 따라 갈립니다.

| 시설 | 연결 |
|---|---|
| 공장·제조, 물류·창고, 데이터센터, 농축수산, 에너지·발전, 건설·현장 | **에너지 탐사** (기존 앱) |
| 병원·제약·바이오, 상업용 건물, 소형·중형·대형 건물 | **시설 에너지 탐사** (신규 · 준비 중 표시) |

준비 중인 신규 어플리케이션은 `시설 에너지 탐사` · `소형 건물` · `중형 건물` · `대형 건물` 4개입니다.
에디터에서 같은 id 로 어플리케이션을 만들면 **자동으로 정상 상세 페이지로 전환**됩니다.

| 표시 이름 | 만들 때 쓸 id |
|---|---|
| 시설 에너지 탐사 | `facility-energy` |
| 소형 건물 | `bldg-small` |
| 중형 건물 | `bldg-medium` |
| 대형 건물 | `bldg-large` |

나머지 93개 조합은 전부 기존 앱으로 연결돼 지금 바로 정상 동작합니다.

---

## 새로 되는 것

### 1. 파인더 항목 편집 — `/editor` → 🧭 파인더 항목

Q1(시설)·Q2(고민) 을 **이름 변경 · 추가 · 삭제 · ≡ 드래그 순서변경** 할 수 있습니다.
시설마다 어떤 고민을 보여줄지도 체크로 정합니다.
저장하면 `SiteContent` 시트의 `finder.config` 행에 JSON 으로 들어갑니다.

- **저장된 값이 없으면 지금과 완전히 똑같이 동작합니다** (배포해도 화면이 안 바뀝니다)
- 매칭 그리드(🎯)도 이 목록을 자동으로 따라갑니다
- 내부 ID 는 영문·숫자로 자동 생성됩니다 (한글 이름이어도 `fac-a1b2` 형태)
- ID 는 한 번 정하면 바꾸지 마세요 — 바꾸면 기존 매칭이 끊깁니다

### 2. 준비 중 화면 — 네 곳 모두 같은 모양

내용이 아직 없는 페이지는 빈 화면으로 튕기지 않고 아래 화면을 보여줍니다.
제목만 상황에 맞게 바뀌고 나머지는 동일합니다.

| 어디서 | 제목 |
|---|---|
| 파인더에서 온 활용분야 | `중형 건물 전력·에너지 절감` (고른 조합 그대로) |
| 활용분야 직접 진입 | `시설 에너지 탐사` (앱 이름) |
| 도입 사례 없음 | `○○ 도입 사례` |
| 프로모션 상세 없음 | 프로모션 제목 |

구성은 `준비 중` → 제목 → 두 문장 → **무료 현장 진단 신청 / 전화 상담** → `진단 무료` → 둘러보기 링크.

**"무료 현장 진단 신청"** 을 누르면 상담 폼으로 가면서 시설 유형이 자동 선택되고
문의 내용에 맥락이 미리 채워집니다 (예: `중형 건물 · 전력·에너지 절감 관련 구성과 예상 비용 문의`).

나중에 에디터에서 해당 내용을 채우면 **자동으로** 정상 페이지로 바뀝니다.

### 3. 사진 순서 드래그

사진 칸마다 `≡` 를 끌어 순서를 바꿀 수 있고, 맨 위가 `항목 1 · 대표` 로 표시됩니다.

### 4. 고쳐진 버그 2개

- **캡션 유실** — 업로드용 파일 칸이 URL·캡션 사이에 끼어 있어, 저장할 때마다 사진 캡션이 조용히 지워지던 문제
  (어플리케이션·프로모션·설치사례·도입사례 등 사진이 들어가는 모든 섹션에 영향)
- **순서 드래그** — 두 번째 드래그부터 엉뚱한 자리로 가던 문제

---

## 배포 후 확인

1. 강력 새로고침 (`Cmd/Ctrl + Shift + R`)
2. 메인 → 솔루션 파인더 **시설 11개** (상업용 건물 · 소형/중형/대형 건물 포함)
3. `/editor` 상단에 **🧭 파인더 항목** 버튼이 보이는지

## Apps Script 갱신은 선택 사항입니다

**모든 기능이 지금 배포된 저장 서버 그대로 동작합니다.** 에디터가 저장 서버 버전을
자동으로 확인해서, 옛 버전이면 아래 방식으로 같은 결과를 만들어 냅니다.

| 기능 | 새 저장 서버 | 지금 서버 (호환 모드) |
|---|---|---|
| 삭제 | 행을 실제로 삭제 | 행 내용을 모두 비움 → 사이트에서 사라짐, 시트엔 빈 줄이 남음 |
| 순서 저장 (프로모션 등 `order` 열 있는 탭) | 행을 옮김 | `order` 값을 1,2,3… 으로 고쳐 씀 (행을 안 옮겨 더 안전) |
| 순서 저장 (`order` 열 없는 탭) | 행을 옮김 | 각 행을 새 위치에 다시 씀 (확인 창이 먼저 뜹니다) |

버전 확인은 토큰 없이 `whoami` 를 한 번 물어보는 방식이라 **시트를 전혀 건드리지 않습니다.**

Apps Script 를 갱신하면 삭제 시 빈 줄이 안 남는 정도의 차이만 있습니다.
회사 계정 정책(This app is blocked)으로 갱신이 어렵다면 그냥 두셔도 됩니다.
