# 설치 사례(The Field Review) — 히어로 배너 + 웹/모바일 최적화 · 2026-09-16

대상: https://monnit.co.kr/installation-photos
이 폴더는 `node build.js` 까지 돌려 검증한 **전체 빌드 통합본**입니다. (빌드 결과: 정적 페이지 117개, 라우터 정합성 OK)

## 변경 파일
| 파일 | 내용 |
|---|---|
| `installation-photos.html` | 히어로 배너, 반응형 CSS, 접근성·터치 스크립트, OG 이미지 교체 |
| `images/field-review/*` (신규 10개) | 문구 포함 배너 `hero-v2-*`(WebP 800/1200/1600/2400 + JPG 1600), 모바일·태블릿용 2:1 크롭 `hero-m-*`, OG 1200×630 |
| `_headers` | `/images/field-review/*` 30일 캐시 |
| `robots.txt`, `sitemap.xml` | build.js 재생성(날짜만 변경) |

## 히어로 배너
- 콘텐츠 폭 안에 배치, 하단이 배경색(#0C1220)으로 자연스럽게 이어짐
- 로드 후 페이드인 (reduced-motion 환경은 즉시 표시)
- 원본 PNG 8.8MB → 화면 폭·해상도별 자동 선택 (모바일 48~88KB, 데스크톱 59~162KB)
- 배너 문구: "MONNIT KOREA" / "모든 선택에는, 분명한 이유가 있다." (v2 이미지)
- 900px 이상: 문구가 들어간 v2 이미지 그대로 사용
- 899px 이하(모바일·태블릿): 봉황 중심 2:1 크롭 + 같은 문구를 HTML 텍스트로 얹어 작은 화면에서도 읽히게 처리 (이미지 속 글자가 너무 작아지는 문제 방지)
- `<link rel=preload>` + `fetchpriority=high` 로 첫 화면(LCP) 가속

## 웹/모바일 최적화
- **필터 바 겹침 수정**: 스크롤 시 필터 바가 상단 MONNIT 바 밑으로 숨던 문제 → 사이트바 실제 높이만큼 아래에 고정
- **접힌 필터**: [필터] 버튼과 섹터 줄을 한 줄로 합쳐 고정 영역 높이 축소
- **모바일 필터**: 섹터·센서 칩을 한 줄 가로 스크롤로 (선택한 칩은 자동으로 가운데 정렬)
- **모바일 통계**: 360px에서 3번째 수치가 줄바꿈되던 문제 → 3칸 고정
- **모바일 타이포·여백** 정리, 검색 안내 문구 축약, iOS 확대 방지(16px)
- **카드 사진 스와이프**: 터치로 좌우 넘기기 (기존엔 마우스 호버에서만 전환)
- 터치 기기에서 카드 호버 튐 제거, 노치/홈바 safe-area 대응
- 카테고리 키커 한글 자간 과다 완화
- 접근성: 검색창 라벨, 필터 칩·사진 키보드 조작(Enter/Space), aria-pressed
- 공유 미리보기(OG/Twitter) 이미지를 새 배너로 교체, theme-color 지정

## 검증
360 / 390 / 768 / 1024 / 1440px 에서 가로 넘침 없음, 스크립트 오류 없음, 필터·검색·라이트박스 정상 동작 확인.

## 적용
`Monnit-main` 안의 내용을 저장소 루트에 덮어쓰고 커밋·푸시하면 됩니다. (.git 은 건드리지 마세요)
```bash
rsync -a Monnit-main/ /경로/내-저장소/ --exclude '.git' --exclude 'node_modules'
git add -A && git commit -m "feat(installation-photos): 한옥 히어로 배너 + 웹/모바일 최적화" && git push
```
