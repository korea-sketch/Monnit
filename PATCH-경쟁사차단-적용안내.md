# 경쟁사 차단 · 방문 기록 적용 안내 (2026-09-16)

## 한 줄 요약

데키스트(dekist.com)가 제안 가이드를 반복해서 받아 가던 경로를 막고,
앞으로 그 회사가 쓰는 IP를 **자동으로 수집해 차단**합니다.
사이트 방문을 **IP별로 모두 기록**하고, 특정 IP를 **화면(/ops/block)이나 Claude에게 말로** 막을 수 있습니다.

## 0. 배포 전에 할 일 — 환경변수 1개

Netlify → Site configuration → Environment variables 에 추가하고 재배포하세요.

| 키 | 값 |
|---|---|
| `BLOCK_API_KEY` | Claude 가 알려드린 키 (`mbk_` 로 시작) |

이 키가 없으면 원격 차단 API(`/api/block`)만 꺼지고, 나머지(자동 차단·방문 기록·/ops/block)는 그대로 동작합니다.
키가 새어 나갔다고 생각되면 값만 바꾸고 재배포하면 예전 키는 즉시 무효가 됩니다.

---

## 1. 먼저 알아야 할 것 — 회사 IP는 밖에서 찾을 수 없습니다

| 조회 항목 | 결과 | 쓸 수 있나 |
|---|---|---|
| www.dekist.com | 133.186.143.57 (클라우드 웹서버) | ✗ 사무실 IP 아님 |
| dekist.com 메일 | Google Workspace | ✗ 구글 공용 서버 |
| 사무실·직원 휴대폰 IP | 공개 정보 없음 (통신사 유동 IP) | — |

게다가 지금까지 리드 원장에는 **접속 IP가 기록되지 않았습니다.**
그래서 이번 패치는 「데키스트가 자기 이메일·회사명·전화번호를 넣고 들어온 순간의 IP」를
자동으로 잡아서 막는 방식입니다. 한 번 잡힌 IP로는 지메일 같은 다른 주소로 위장해도 막힙니다.

## 2. 더 급했던 문제 — 폼을 거치지 않고 받을 수 있었음

`/files/monnit-predictive-maintenance-guide.pdf` 가 **공개 주소로 열려 있었습니다.**
게이트(proposals/)에 있는 제안 가이드와 똑같은 파일이고, 랜딩 페이지 소스에
「예비 경로」로 주소가 그대로 적혀 있었습니다. 개발자도구만 열면 누구나 받을 수 있었습니다.
온도 제안서 3종(`/files/monnit-temp-*-guide.pdf`)도 같은 상태였습니다.

→ `_redirects` 에서 4개 모두 404로 막고, 랜딩 페이지의 예비 경로를 지웠습니다.
링크 발급이 실패하면 이제 파일 대신 「담당자가 메일로 보내드리겠습니다」가 표시됩니다.

## 3. 무엇을 기준으로 막나

| 기준 | 기본값 | 걸리면 |
|---|---|---|
| 이메일 도메인 | dekist.com | 자료 링크·안내 메일·접수 모두 무시 + IP 자동 수집 |
| 회사명 | 데키스트 / dekist (띄어쓰기·(주) 무시) | 〃 |
| 전화번호 | 010-7502-4359 | 〃 |
| 접속 IP | 자동 수집분 + 직접 추가분 | 아래 범위대로 |

**IP 차단 범위**

| 범위 | 효과 | 자동 수집 시 |
|---|---|---|
| 사이트 전체 | 어떤 페이지도 열리지 않음 (403) | PC 접속 → 30일 |
| 자료·접수만 | 페이지는 보이지만 자료 링크·접수가 조용히 무시됨 | 휴대폰 접속 → 3일 |

휴대폰 통신사 IP는 수천 명이 같이 쓰고 금방 다른 사람에게 넘어갑니다.
사이트 전체를 막으면 광고로 들어온 진짜 고객까지 막히므로 휴대폰은 짧고 약하게 막습니다.

**차단은 조용히 합니다.** 상대에게는 「자료 준비 중」·「링크 만료」와 같은 평범한 화면만 보입니다.
차단 사실을 알리면 바로 다른 주소·다른 망으로 바꿔 오기 때문입니다.
차단된 신청은 리드 원장·먼데이·알림 메일에 올라가지 않고 `/ops/block` 에만 남습니다.

## 4. 관리 화면 — /ops/block

관제 화면(/ops) 상단의 **차단 관리** 버튼으로 들어갑니다. 로그인은 관제 화면과 같습니다.

- **차단 추가** : IP(1.2.3.4 · 1.2.3.0/24 · 1.2.3.*) / 도메인 / 회사명 / 전화번호
- **차단 규칙** : 자동 수집분을 「영구 확정」하거나 「해제」
- **차단된 시도** : 언제, 어느 IP에서, 어떤 이메일로 시도했는지
- **최근 리드** : 새로 들어온 리드의 IP가 보이고, 수상하면 [IP] [도메인] [회사] [전화] 버튼으로 바로 차단

gmail·naver 같은 일반 메일 도메인은 통째로 막을 수 없게 해 두었습니다.
변경은 1분 안에 사이트 전체에 반영됩니다. 담당자 알림 메일에도 이제 **접속 IP** 줄이 붙습니다.

코드를 고치지 않고 추가하려면 Netlify 환경변수도 쓸 수 있습니다(재배포 필요, 쉼표 구분):
`BLOCK_IPS` · `BLOCK_DOMAINS` · `BLOCK_COMPANIES` · `BLOCK_PHONES`

## 4-1. 방문 기록 (IP별)

페이지를 열 때마다 **IP · 시각 · 페이지 · 유입 경로 · 지역(구/시) · 기기**가 한 건씩 남습니다.
자료 다운로드(getdoc)도 「[자료 다운로드] 파일명」으로 같이 남습니다. 90일 보관.

- `/ops/block` → **방문 기록** : 날짜를 고르면 그날 들어온 IP 목록(페이지뷰·지역·기기·마지막 페이지)
- IP 옆 **기록** : 그 IP가 최근 30일 동안 본 페이지 전체 → **사이트 전체 차단 / 자료·접수만 차단 / 해제**
- 리드로 접수한 적 있는 IP는 회사명이 노란색으로 붙습니다 — 「누가 무엇을 보고 갔는지」 연결
- 검색엔진 로봇(구글·네이버 등)은 기록하지 않습니다. curl·파이썬 같은 수집 스크립트는 기록합니다.
- 기록을 끄려면 환경변수 `VISIT_LOG=off`

## 4-2. Claude 에게 말로 차단 — /api/block

채팅에서 이렇게 말하면 Claude 가 `/api/block` 을 호출해 바로 처리합니다.

| 이렇게 말하면 | Claude 가 하는 일 |
|---|---|
| 「1.2.3.4 차단해」 | 사이트 전체 영구 차단 |
| 「1.2.3.4 자료만 막아, 30일」 | 자료·접수만 30일 차단 |
| 「1.2.3.4 풀어줘」 | 차단 해제 |
| 「1.2.3.4 뭐 봤어?」 | 최근 30일 방문 페이지·접수 이력 |
| 「오늘 방문자 IP 보여줘」 | 오늘 방문 IP 목록 |
| 「데키스트 IP 찾아서 막아」 | 리드·차단 기록에서 IP 검색 → 차단 |
| 「○○.com 도메인 막아」 | 이메일 도메인 차단 |

내부망(192.168.x 등)·너무 넓은 범위(/8 미만)·gmail 같은 일반 메일 도메인은 API에서도 거부합니다.
코드 기본값(dekist.com 등)은 API로 풀 수 없습니다.

## 5. 바뀐 파일

| 파일 | 내용 |
|---|---|
| `netlify/functions/_guard.js` | **신규** 차단 규칙·IP 자동 수집·차단 기록 |
| `netlify/functions/blockadmin.mjs` | **신규** /ops/block 관리 화면 (방문 기록 포함) |
| `netlify/edge-functions/block-ip.js` | **신규** 사이트 전체 IP 차단 + 방문 기록 (오류 시 자동 통과) |
| `netlify/functions/_visits.mjs` | **신규** 방문 기록 조회·요약·90일 정리 |
| `netlify/functions/blockapi.mjs` | **신규** 원격 차단 API `/api/block` (키 인증) |
| `netlify/functions/sendpw.js` | 자료 링크 발급 전 차단 검사 |
| `netlify/functions/getdoc.js` | 차단 IP에서는 남의 링크도 안 열림 · 다운로드를 방문 기록에 남김 |
| `netlify/functions/sendguide.js` | 가이드 메일 발송 전 차단 검사 |
| `netlify/functions/lead.mjs` | 차단 검사 + 리드에 접속 IP 기록 |
| `netlify/functions/_notify.mjs` | 알림 메일에 「접속 IP」 추가 |
| `netlify/functions/_ops_ui.mjs` | 관제 화면 상단에 「차단 관리」 버튼 |
| `_redirects` | /files/ 공개 PDF 4개 차단, 이 안내문 차단 |
| `promo-proposal.html` · `promo/proposal/index.html` | 공개 예비 경로 제거 |
| `promo/temperature/index.html` | 공개 예비 경로 제거 |

압축을 저장소 루트에 그대로 풀어 덮어쓰고 커밋하면 됩니다. (환경변수는 0번 참고)

## 6. 배포 후 확인

1. `https://monnit.co.kr/files/monnit-predictive-maintenance-guide.pdf` → 404 가 나와야 합니다.
2. 제안 가이드 랜딩에서 평소 이메일로 신청 → 정상 다운로드.
3. `/ops/block` 접속 → 기본 규칙 3건(도메인·회사명·전화)이 보이면 정상.
4. 배포 로그에서 Edge Functions 항목에 `block-ip` 가 보이면 사이트 전체 차단·방문 기록이 켜진 것입니다.
5. 사이트를 몇 페이지 열어 본 뒤 `/ops/block` → 방문 기록에 내 IP가 보이면 정상입니다.
6. Claude 에게 「차단 API 연결 확인해」 라고 하면 `/api/block` 상태를 점검합니다.

## 7. 한계

- 과거 접수(오늘 18:20 데키스트 건 포함)는 IP가 남아 있지 않아 소급 차단이 안 됩니다.
  **다음 시도부터** 자동으로 잡힙니다.
- 개인 휴대폰 + 개인 메일 + 가짜 회사명으로 오면 기술적으로 구분할 수 없습니다.
  /ops/block 의 「최근 리드」에서 IP를 보고 수동으로 막아야 합니다.
- 회사 밖(집·카페)에서 새 IP로 페이지를 보는 것 자체는 막을 수 없습니다. 방문 기록으로 뒤늦게 확인은 됩니다.
- 방문 기록은 페이지뷰마다 저장소에 한 건씩 씁니다. 방문량이 크게 늘면 Netlify 사용량을 확인하세요.
- 차단·해제는 최대 1분 뒤 반영됩니다(규칙 캐시).
- 개인정보처리방침에는 접속 IP·방문 기록 수집이 이미 적혀 있습니다. 이용 목적에 「보안·부정 이용 방지」, 보유 기간에 「90일」이 들어가 있는지만 확인하세요.
- 알리미·SCUBE·회사소개서 PDF(`/files/alarm-service-4types.pdf` 등)는 자동 응답 메일에서
  공개 주소로 쓰고 있어 그대로 두었습니다.
