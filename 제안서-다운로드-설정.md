# 제안서 다운로드 — 이메일 게이트

## 무엇이 바뀌었나

**전** — 다운로드 주소가 구글 시트 `url` 열에 있었고, 그 시트를 브라우저가 공개 CSV 로 통째로 내려받았습니다. `F12` → 콘솔에 `WHITEPAPERS` 를 치면 16개 주소가 전부 보였고, 이메일을 넣지 않아도 다 받아갈 수 있었습니다.

**후** — PDF 16개가 저장소 `proposals/` 안에 있고 **공개 주소가 아예 없습니다.** 이메일을 확인한 뒤 서버가 10분짜리 서명 링크를 1건만 발급합니다.

## 흐름

```
제안서 선택 + 이메일 입력
        ↓
sendpw 함수 : 이메일 형식 검증 → 제목으로 파일 조회
        ↓
10분 유효 서명 링크 발급 (HMAC-SHA256)
        ↓
getdoc 함수 : 서명·만료 확인 → PDF 전송
        ↓
같은 도메인 첨부파일 → 새 창 없이 바로 저장
```

## Jay 가 할 일

**없습니다.** 환경변수도, 구글 드라이브도, 시트 수정도 필요 없습니다. 그대로 배포하시면 동작합니다.

선택 사항 2가지:

| 항목 | 내용 |
|---|---|
| `DL_SECRET` | Netlify 환경변수. 링크 서명 키. 설정 안 하면 코드 기본값을 씁니다. 운영에서는 임의 문자열로 설정 권장 |
| 시트 `url` 열 | 이제 코드가 읽지 않지만, 예전 링크가 남아 있다면 지워두는 편이 깔끔합니다 |

`BREVO_API_KEY` 는 기존 그대로. 없어도 다운로드는 정상 동작하고 메일만 건너뜁니다.

## 추가·교체 방법

새 제안서를 넣을 때:

1. PDF 를 `proposals/` 에 넣습니다 (파일명은 영문 소문자·하이픈)
2. `netlify/functions/_docmap.js` 의 표에 한 줄 추가

```js
'새 제안서 제목' : ['new-proposal.pdf', '모넷코리아_새제안서.pdf'],
```

왼쪽은 사이트에 보이는 제목, 가운데는 실제 파일명, 오른쪽은 사용자가 받을 때 보일 이름입니다. 제목의 가운뎃점·공백 차이는 코드가 자동으로 흡수합니다.

## 바뀐 파일

| 파일 | 내용 |
|---|---|
| `proposals/` | 신규 — PDF 16개 (23.5 MB) |
| `netlify/functions/_docmap.js` | 신규 — 제목→파일 매핑 (서버 전용) |
| `netlify/functions/getdoc.js` | 신규 — 서명 검증 후 PDF 전송 |
| `netlify/functions/sendpw.js` | 서명 링크 발급 + 안내 메일 |
| `netlify.toml` | `included_files = ["proposals/**"]` — 함수 번들에 PDF 포함 |
| `_redirects` | `/proposals/*` 직접 접근 404 차단 |
| `app.js` `wpRequest()` | 서버 응답으로 다운로드 |
| `app.js` `mapWhitepapers()` | 시트 `url` 열을 읽지 않음 |

## 검증 (로컬에서 실제 실행)

| 테스트 | 결과 |
|---|---|
| 정상 신청 → 링크 발급 | 통과 |
| 발급 링크로 PDF 수신 (1.47 MB, `%PDF` 확인) | 통과 |
| 이메일 형식 오류 차단 | 400 |
| 토큰 없이 요청 차단 | 401 |
| 서명 위조 차단 | 403 |
| 만료 링크 차단 | 410 |
| 파일명 바꿔치기 차단 | 403 |
| 경로 탈출(`../app.js`) 차단 | 400 |
| 16개 제목 전부 파일 전송 | 16/16 |

## 비용

Netlify 무료 한도는 대역폭 100 GB/월, 함수 호출 125,000회/월입니다.
제안서 1건이 1.5 MB 이므로 **월 약 68,000건까지 무료**입니다. 월 100건이면 150 MB — 한도의 0.15%.
현재 플랜과 사용량은 Netlify 대시보드 Usage 에서 확인하세요.

## 남은 한계

- 받은 사람이 **10분 안에** 링크를 남에게 넘기면 그 사람도 받을 수 있습니다. 그 뒤로는 만료됩니다.
- 이메일을 바꿔가며 16번 신청하면 16개를 다 받을 수 있습니다. 대량 자동 수집을 막는 구조이지, DRM 이 아닙니다.
- 저장소가 23.5 MB 늘어납니다. 배포 시간이 조금 길어질 수 있습니다.
