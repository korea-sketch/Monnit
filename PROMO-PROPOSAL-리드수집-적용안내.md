# 제안 가이드 랜딩 — 자료 게이트 + 리드 수집

`/promo/proposal` 을 **정보를 남겨야 받는 구조**로 바꿉니다.
사이트에 이미 있는 규격(`js/monnit-lead.js` · `sendpw` · `getdoc` · `/api/lead`)만 씁니다.
새 서비스도, 새 환경변수도 없습니다.

## 무엇이 바뀌나

**전** — 버튼을 누르면 `/files/monnit-predictive-maintenance-guide.pdf` 가 그대로 내려갔습니다.
공개 주소라 누구에게나 퍼질 수 있고, 누가 받아갔는지 남지 않았습니다.

**후** — 다른 제안서 16종과 **똑같은 방식**이 됩니다. 회사명·이름·회사이메일을 받고,
`sendpw` 가 10분짜리 서명 링크를 1건 발급합니다. 접수는 `/api/lead` 원장에 남아 관제(ops) 화면에 뜹니다.

## 흐름

```
광고 클릭 → 랜딩 → 미리보기로 가치 확인 → [PDF 받기]
                                            ↓
                        폼 (회사명·이름·회사이메일 필수 3개)
                                            ↓
        ① sendpw → 10분 서명 링크 → getdoc → PDF 전달 (+ Brevo 안내메일)
        ② Web3Forms → 담당자 알림 메일
        ③ MonnitLead.track('doc_request') → /api/lead → 관제 화면
                                            ↓
                              완료 화면 + 전화 CTA
```

**자료는 ②③ 결과와 무관하게 먼저 전달됩니다.** 서명 링크 발급이 실패하면
예비 경로(`/files/...`)로라도 반드시 드립니다. 광고비가 들어간 방문을 빈손으로 보내지 않습니다.

## 바뀐/추가된 파일

| 파일 | 내용 |
|---|---|
| `promo-proposal.html` | 교체 — 게이트 폼 + 사이트 리드 규격 연결 |
| `promo/proposal/index.html` | 위와 동일 사본 (기존과 같은 관행) |
| `proposals/predictive-maintenance-guide.pdf` | **신규** — 게이트 대상 자료 (1.4 MB) |
| `netlify/functions/_docmap.js` | 표에 한 줄 추가 |
| `images/proposal-guide/*.jpg` | 교체 — 표지에 QR 추가된 판 + `og-image.jpg` 신규 |
| `files/monnit-predictive-maintenance-guide.pdf` | 교체 — 연락처 반영판 (예비 경로용) |

`netlify.toml` · `_redirects` 는 **손댈 것 없습니다.** `/proposals/*` 차단과
`included_files = ["proposals/**"]` 가 이미 되어 있어 새 PDF도 그대로 보호됩니다.

`_docmap.js` 추가분:

```js
'무선 IoT 설비 예지보전 제안 가이드': ['predictive-maintenance-guide.pdf',
                                       '모넷코리아_무선IoT_설비예지보전_제안가이드.pdf'],
```

랜딩의 `CONFIG.DOC_TITLE` 이 이 키와 **글자 하나까지 같아야** 합니다. 다르면 `not_ready` 로 떨어집니다.

## 폼 항목

| 항목 | 필수 | 관제 화면 칸 |
|---|---|---|
| 회사명 | ● | 회사 |
| 이름 | ● | 담당자 |
| 회사 이메일 | ● | 이메일 — 서명 링크 발급 기준 |
| 연락처 | | 연락처 |
| 감시 검토 중인 설비 규모 | | 설비 |
| 개인정보 수집·이용 동의 | ● | 마케팅 수신 |

`문의 사항` 칸에는 이렇게 들어갑니다:

```
[A·78점] 설비 규모: 30 ~ 50대 · 체류 95초 · 스크롤 82% · 미리보기 2회 · 방문 1회 · 모바일
```

**리드 등급이 맨 앞에 옵니다.** 관제 화면을 열자마자 누구부터 전화할지 보입니다.
등급 산식은 설비규모(최대 40) + 회사 이메일(12) + 연락처(15) + 체류시간(최대 15)
+ 스크롤 70%↑(8) + 미리보기 열람(8) + 재방문(6). 65점↑ A, 38점↑ B.

`출처`(utm·gclid·fbclid)와 `접점`은 `MonnitLead` 가 자동으로 채웁니다.
세션 최초 유입이 보존되므로 광고로 들어와 나중에 신청해도 소재가 추적됩니다.

## 전화만 걸고 나가는 사람

`tel:` 링크를 누르면 `contact` 유형으로 한 건 남깁니다
(`회사명: (전화 클릭)`, 문의 사항에 체류·스크롤·기기). 광고비가 들어간 방문이므로
번호를 눌렀다는 사실만이라도 남겨 둡니다. 한 세션에 한 번만 기록합니다.

## 전환율을 지키려고 넣은 것

정보를 요구하면 다운로드 수는 반드시 줄어듭니다. 그래서:

- 필수 3개만. 연락처·설비규모는 선택
- 폼 안에 표지 썸네일 — 지금 뭘 받는지 다시 확인시킵니다
- **"영업 전화로 괴롭히지 않습니다"** 안심 문구
- 미리보기 3장을 먼저 확대해 볼 수 있어, 폼을 만나기 전에 가치를 확인합니다
- **한 번 남긴 사람은 90일간 다시 안 묻습니다** (`REMEMBER_DAYS`). 재방문자는 바로 받습니다
- 전화 CTA는 무게이트
- 모바일은 하단 시트 UI · 입력창 16px(iOS 확대 방지) · autocomplete 속성

## 배포 전 확인

- [ ] `proposals/predictive-maintenance-guide.pdf` 커밋됨 (저장소 1.4 MB 증가)
- [ ] `_docmap.js` 한 줄 추가됨
- [ ] `images/proposal-guide/` 9개 파일 교체·추가됨
- [ ] 배포 후 `/promo/proposal` 에서 폼 제출 → PDF 수신 → ops 화면에 행 생성까지 관통 확인
- [ ] `/proposals/predictive-maintenance-guide.pdf` 직접 접근 시 404 인지 확인

## 참고 — 지금 라이브에서 잘못된 것

`og:image` 가 `/images/why-ai.jpg` 로 덮여 있었습니다. 카톡 공유 시 엉뚱한 이미지가 떴습니다.
이번 파일에서 `/images/proposal-guide/og-image.jpg` 로 고쳤습니다.
CMS 쪽에서 og 태그를 덮어쓰는 로직이 있다면 그쪽도 함께 확인해 주세요.

## 비용

자료 1건 1.4 MB. Netlify 무료 한도(대역폭 100 GB/월) 기준 월 약 71,000건까지 무료입니다.
월 500건이면 700 MB — 한도의 0.7%.
