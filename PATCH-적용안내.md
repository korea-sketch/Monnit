# Monnit-main (35) 통합 패치 (2026. 8. 26.)

35 소스에 이미 있던 것: flame 직링크 전용(목록·sitemap 제외, noindex, _headers)
이번에 추가된 것:

## 1. HTML 주석 제거 (소스 노출 방지)
- 전 페이지(136개)에서 내부 구조·작업 메모 주석 삭제 — SPA 템플릿 내부 포함
- build.js에 자동 제거 단계 추가 (빌드 완료 후 실행) → 재빌드해도 유지
- 실제 JS·CSS는 보호 · editor.html 제외 · 주석 원본은 바탕화면 백업 폴더에

## 2. 메인 로고 깨짐 수정
- 로고 30일 캐시 → 옛/손상 캐시 브라우저에서 깨져 보이던 문제
- /images/img-01.png?v=2 버전 부여 + 실패 시 캐시 우회 1회 자동 재시도

## 3. 캐시 버전 118 → 119
- 35에서 app.js가 또 수정되어 있어 한 번 더 올림 (전 페이지 일관)
- 규칙: app.js 고치면 index.html의 ?v= 숫자 +1 후 배포

## 검수 결과 (전부 통과)
주석 0 · NUL 0 · 로고 v2+재시도 · v119 전 페이지 일관 · flame: 목록 비노출 /
직링크 정상 / sitemap 0건 / noindex 2중 · gtag·쿠키동의 스크립트 원형 유지

## 5. 프로모션 카드 이미지 깨짐 폴백 (2026. 8. 26. 추가)
- 원인: 프로모션 목록 카드는 구글시트(CMS)의 이미지 주소를 쓰는데,
  그 주소(외부 이미지 변환 URL)가 간헐적으로 실패 → 깨진 아이콘 노출
- 조치: 이미지 로드 실패 시 ① 코드에 내장된 로컬 이미지(예: consulting →
  /images/promo-consulting-banner.jpg)로 자동 교체 ② 그것도 없으면 이미지를
  숨겨 깨진 아이콘 대신 카드 배경이 자연스럽게 보이게
- 근본 해결하려면: 프로모션 시트의 image 칸을 사이트 내 경로
  (/images/promo-….jpg)로 바꿔두면 외부 의존이 사라집니다
- app.js 변경에 따라 캐시 버전 119 → 120

## 6. 레지던스 프로모션 정리 + 에디터 동기화 (2026. 8. 26. 추가)
- "한 달 한정 접수" 문구 전부 제거:
  카드 제목 · 랜딩 og설명 · 랜딩 D-day 줄 · 배너 이미지(webp/jpg)에 박힌 필까지
  이미지에서 자연스럽게 지움 (바닥 질감 복원)
- editor.html 내장 목록에 residence · flame-reservation 추가
  → 이제 에디터 드롭다운에 "사이트 내장(시트에 없음)"으로 표시되어 시트로 옮겨 관리 가능
- app.js 변경에 따라 캐시 버전 120 → 121
