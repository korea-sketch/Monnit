# 맞춤 제안서 자동 발송 — 적용 안내 (2026-09-18 · v7)

## 한 줄 요약

고객이 **어느 입구로 들어오든**(홈 솔루션 파인더 · 상담 신청 폼 · 빠른 상담 · 자료실 · 메뉴 · `/proposal`),
회사명·이메일·문의 내용으로 업종과 세부 업종을 자동으로 파악하고, **130여 개국 Monnit 글로벌 레퍼런스 · 공공기관·대기업을 포함한 국내 도입 현장 · 산업별 플레이북**을 대조해
**맞춤 제안서 PDF(약 10쪽, 첫 장 한옥 배너 고정)** 를 만들어 메일로 보냅니다. 내부적으로는 신청 후 2~3분 안에 나가고, **고객 화면에는 「몇 시간 이내 · 접수 순서대로」** 로만 안내합니다.
판단이 어려운 건만 자동으로 **엔지니어 확인 후 발송(영업일 1일 안)** 으로 넘어갑니다.
신청·진행 현황 화면은 별도 페이지가 아니라 **사이트 안의 화면(SPA 뷰)** 으로 붙었습니다 — 헤더·푸터·검색·편집기·다국어가 그대로 적용됩니다.
**무료 제안서는 가장 고민되는 과제 1개 기준**이고, 다른 과제·현장 전체 구성·견적은 **견적 요청(상담)** 으로만 받습니다.
사이트 전체(신청·진행 화면·상담 폼 포함)가 **영문(EN)** 으로도 보이고, 사례를 보러 나가도 화면 아래 **「내 맞춤 제안서」 버튼**으로 한 번에 돌아옵니다.
관제(`/ops`)에서는 **탭**으로 맞춤 제안서 · 발송 대장 · 고객 인사이트를 오가고, AI 재생성은 **Claude 재생성 → 확인 → 발송 여부 결정** 순서로 처리합니다.
`/proposal` 에는 **「대화로 신청」 탭**이 붙어, 단계를 고르는 대신 **대화로 회사·성함·이메일·현장·고민을 받아 같은 제안서**를 만듭니다 — 기본 대화는 **코드(규칙)** 가 처리해 **AI 비용이 들지 않고**, 규칙이 못 알아들은 말일 때만 **가장 싼 모델**을 한 번 부릅니다.

---

## 0000. v6 — 프로모션 연결 (컨설팅 신청 → 예약 · 백서 → 맞춤 제안서)

### ① 컨설팅 신청(/promo/consulting) → 현장 진단 예약(/visit)

```
신청 폼 제출 → 감사 화면(?submitted=1, 전환 집계 그대로)
   ① 무료 체험 신청 ✓   ② 현장 진단 날짜(지금 1분)   ③ 방문·센서 설치
   [현장 진단 날짜 고르기 →]  ← 5초 뒤 자동 이동 (스크롤·터치·키 입력 시 멈춤)
   「나중에 예약할게요」 → 머무름, 담당자 전화로 일정 결정
        ↓
/visit — 위쪽에 「무료 체험 신청 접수 → 이어서 날짜를 골라 주세요」
   회사·담당자·연락처·이메일은 한 줄 요약으로 접힘(「수정」으로 펼침)
   설비 칸 = 라인 + 설비 위치 + 주요 회전설비, 진동센서 경험도 미리 선택
   → 날짜·시간 → 방문 주소·확인할 것·공정만 적고 예약
```

- 신청값은 **주소가 아니라 이 탭 저장소(sessionStorage)** 로 넘깁니다. 6시간이 지나면 쓰지 않고, 예약이 끝나면 지웁니다.
- 예약 메일·관제 원장에 **「연결 신청: 무료 체험 신청 후 이어서 예약」** 이 붙어 같은 고객임을 바로 알 수 있습니다.
- 처음 쓰는 분(무경험)에게 가이드 메일을 보내는 기존 동작은 그대로입니다.
- GA 이벤트: `visit_booking_start`(이동) · `visit_booking_later`(나중에) · `visit_booking_view`(예약 화면 도착) · `visit_booked`(예약 완료).
- 참고: 예약 화면 일정 설정(`/visit/config`)을 주는 `netlify/functions/visitadmin.mjs` 가 받은 사본에 없습니다. 없으면 기본 일정(평일 09:30~16:00, 72시간 뒤부터)으로 보입니다.

### ② 백서 다운로드 → 「우리 현장 맞춤 제안서도 받기」

| 위치 | 적용 |
|---|---|
| `/promo/modbus` (Modbus 연동 백서) | 관심 구성안 → 시설 유형 기본값, 과제 기본값 「통합관제·연동」 |
| `/promo/proposal` (예지보전 제안 가이드) | 「PDF 받기」 창에서만 보임(상담 문의 창에는 없음). 기본 공장 · 설비 고장 |
| `/promo/temperature` (온도 감시 제안서 3종) | 고른 자료로 시설 유형(제약·방폭·콜드체인)과 과제(온도·냉장) 기본값 |
| `/whitepaper` (사이트 산업별 제안서) | 고른 제안서 이름으로 시설·과제 추정. 선택 칸을 버튼 위로 옮김 |

- 폼 아래에 **「자료만 받기(기본) / 우리 현장 맞춤 제안서도(무료 · 몇 시간 이내)」** 카드형 선택이 붙습니다. 고르면 시설 유형 · 가장 고민되는 주제 하나 · 시설 이름 · 제안서용 동의가 열립니다.
- **자료 받기는 지금과 똑같이** 먼저 진행되고, 맞춤 제안서는 같은 입력값으로 `/api/proposal`(입구 `whitepaper`)에 따로 접수됩니다. 한쪽이 실패해도 다른 쪽은 영향이 없습니다.
- 접수 결과가 폼 아래 카드로 나옵니다: **진행 현황 보기 →** (사이트 진행 화면, 사이트 안에서는 「내 맞춤 제안서」 돌아가기 버튼도 뜸) / 이미 받은 회사면 「진행 화면 링크를 메일로 다시 보냈습니다 · 견적 요청」 / 실패면 「자료는 정상 발송 · 신청 화면에서 다시」.
- 이미 정보를 남긴 재방문자(폼 없이 자료만 다시 받는 경우)에게는 **「우리 현장에 맞춘 제안서도 받아 보세요 →」** 안내 카드가 뜹니다.
- 담당자 알림 메일·관리 화면·발송 대장·인사이트에 입구 **「백서 다운로드」** 와 **「함께 받은 자료」** 가 표시됩니다.
- 공용 모듈: `js/proposal-addon.js` (라이트·다크·사이트 테마, 영문 자동, 입력칸은 `form="mkpa-off"` 로 바깥 폼과 분리 → 기존 폼 전송값·검증에 섞이지 않음).
- `/promo/modbus` 휴대폰(390px)에서 위쪽 메뉴 때문에 가로 스크롤이 생기던 것을 함께 고쳤습니다(좁은 화면에서는 메뉴 링크 숨김).
- **홈 솔루션 파인더 결과 글자 겹침 수정** — 결과 칸이 2단 그리드(861px 이상)인데 맞춤 제안서 안내 문단이 설명 문단과 같은 칸에 놓여 두 문단이 겹쳐 보였습니다. 안내 문단을 버튼 아래 자기 줄로 옮기고 버튼 줄에 `wiz-actions` 클래스를 달아 위치를 고정했습니다(`style.css?v=123`). 1360·1024·768·390px 에서 신청·진행·상담·백서·프로모션·예약 화면의 글자 겹침을 자동 점검해 겹침 없음을 확인했습니다.
- `/promo/consulting` 좁은 화면에서 한글이 글자 중간에서 끊기던 것(「반/납」)도 단어 단위 줄바꿈으로 고쳤습니다.

### ③ 추가·변경 파일 (v6)

| 파일 | 변경 |
|---|---|
| `js/proposal-addon.js` | **새 파일** — 백서 폼용 맞춤 제안서 선택·접수·결과 카드 |
| `promo-consulting.html` | 감사 화면에 예약 단계·자동 이동, 신청값 탭 저장소 전달 |
| `visit.html` · `visit.css` | 넘어온 값 채움 · 요약 접기 · 단계 안내 · 연결 신청 기록 |
| `promo-modbus.html` · `promo-proposal.html` · `promo/temperature/index.html` | 애드온 연결 (`promo/modbus`·`promo/proposal` 사본은 빌드 때 자동 생성) |
| `index.html` · `app.js` · `style.css` | `/whitepaper` 폼 애드온 · 버튼 줄 분리 (`style.css?v=123` · `app.js?v=128`) |
| `netlify/lib/proposal/intake.mjs` · `mail.mjs` · `archive.mjs` · `netlify/functions/proposal-api.mjs` · `proposal-admin.mjs` | 입구 `whitepaper` · 받은 자료 기록·표시 |
| `netlify.toml` | `/js/proposal-addon.js` 캐시 헤더 |
| `tools/proposal-dev.mjs` | `/visit` → `visit.html` (Netlify 규칙과 같게) |
| `tools/test-proposal.mjs` | v7 항목 추가 — 전체 **155개** 통과 |

### ④ 배포 뒤 확인

- [ ] `/promo/consulting` 신청 → 감사 화면에 단계 안내 → 5초 뒤 `/visit` → 회사·연락처 요약, 설비 칸 채워짐 → 예약 → 메일에 「연결신청」
- [ ] 감사 화면에서 「나중에 예약할게요」 → 이동하지 않음
- [ ] `/promo/modbus` · `/promo/proposal` · `/promo/temperature` · `/whitepaper` 에서 「맞춤 제안서도」 선택 → PDF 다운로드 + 결과 카드 → 「진행 현황 보기」 → 제안서 메일 도착
- [ ] 「자료만 받기」로 받으면 지금과 똑같이 동작(맞춤 제안서 접수 없음)
- [ ] 관리 화면 목록에 「백서 · 」 표시, 상세에 「함께 받은 자료」

---

## 000. v5에서 달라진 점

### ① 제안서 첫 장 = 한옥 배너 (시스템 기본값)

- 모든 제안서 PDF의 **첫 장 맨 위에 보내주신 한옥 지붕·봉황 배너**가 들어갑니다. 설정으로 끌 수 없는 기본값입니다.
- 배너·로고는 **함수 코드 안에 넣어서**(`netlify/lib/proposal/brand.data.mjs`, 빌드 때 자동 생성) 이미지 주소·저장소 상태와 상관없이 항상 들어갑니다. 만약 배너를 못 읽으면 그 건은 발송하지 않고 **엔지니어 확인으로 넘어갑니다**(배너 없는 PDF는 나가지 않음). 자동 테스트가 「1쪽에 배너 이미지가 있는지」를 매번 확인합니다.
- 표지 레이아웃도 기본값으로 고정: **배너 → 제안서 번호 → 산업·회사·성함 → 유사 사례 3개 → 과제 → 하단(발행일·제안사·문의·화이트 로고)**, 배너 아래 배경은 **격자·무늬 없는 단색 네이비**입니다(테스트로 확인).
- 배너를 바꾸려면 `data/proposal/brand/cover.jpg` 만 교체하고 배포하면 됩니다(가로 1800px 이상 JPG 권장).

### ② 로고 — 어디에 어떤 것을

| 위치 | 로고 | 파일 |
|---|---|---|
| PDF 표지(어두운 배경) 하단 | **화이트** | `data/proposal/brand/logo-white.png` (코드에 포함) |
| PDF 안쪽 페이지(흰 배경) 오른쪽 위 | **블루** | `data/proposal/brand/logo-blue.png` (코드에 포함) |
| 고객 메일 머리 | **화이트 PNG** (메일 앱은 WebP를 못 읽는 곳이 있어 PNG) | `/assets/brand/monnit-korea-white-480.png` |
| 고객 메일 첫 화면 배너 | 한옥 배너 | `/assets/brand/proposal-cover-1200.jpg` |
| 사이트 공유 미리보기(`/proposal` OG) | 한옥 배너 1200×630 | `/assets/brand/proposal-og.jpg` |
| 사이트에서 쓸 때 | WebP 우선 · PNG 대체 | `assets/brand/monnit-korea-{white,blue}(-480).{webp,png}`, `proposal-cover(-1200).{webp,jpg}` |

- 블루 로고는 받은 JPG가 작아서, 화이트 로고 모양에 블루 JPG의 색(#1168AD · 회색 #C7C8CA)을 입혀 **고해상도로 다시 만들었습니다.** KOREA 의 O 왼쪽 회색 반달도 복원했습니다.

### ③ 발송 시점 표현 — 「약 2영업일 뒤」 → 「몇 시간 이내 · 접수 순서대로」

- 신청 화면·진행 화면·상담 폼·FAQ·SEO 본문·메일에서 정확한 분·초나 「2영업일」을 쓰지 않고 **「몇 시간 이내 · 접수 순서대로 순차 발송」** 으로 통일했습니다. 진행 화면의 mm:ss 카운트다운 대신 **「순차 처리 중」** 표시가 깜빡입니다.
- 실제로는 기존대로 2~3분 안에 나가므로 「생각보다 빨리 왔다」는 경험이 됩니다. 문구는 `PROPOSAL_ETA_TEXT` 로 바꿀 수 있습니다.
- 받은 캡처의 「무료 · 약 2영업일 뒤 PDF」는 **v2 별도 페이지 방식(proposal-bridge.js)** 문구입니다. 이번 파일에는 없으니 `sh 맞춤제안서-이전파일-삭제.sh` 로 옛 파일을 지우면 사라집니다.
- 「진짜 제안서가 필요하면」 흐름: 진행 화면·FAQ·상담 폼 곳곳에서 **견적 요청**으로 이어지고, 회사·성함·제안서 번호가 상담 폼에 채워집니다(개인정보는 주소에 싣지 않음 — ⑦).

### ④ 「27건 대조」 → 글로벌 조직으로 보이는 표현

| 전 | 후 |
|---|---|
| 모넷 설치 사례 27건과 대조 | **130여 개국 Monnit 글로벌 레퍼런스 · 공공기관·대기업을 포함한 국내 도입 현장** 데이터와 대조 |
| 사례 풀 N건 | 개국 Monnit 글로벌 네트워크 (PDF KPI 타일) |
| 홈 사실 칸 | 130+ 개국 글로벌 레퍼런스 · 64,000+ 글로벌 고객사 · 몇 시간 이내 · 무료 |

- 숫자는 사이트 홈에 이미 공개된 값(130+개국 · 64,000+ 고객)을 씁니다. `PROPOSAL_BRAND_COUNTRIES` · `PROPOSAL_BRAND_CUSTOMERS` · `PROPOSAL_BRAND_PUBLIC` 으로 한 번에 바꿀 수 있습니다.
- 「공공데이터 활용」은 실제로 공공데이터를 쓰지 않으므로 넣지 않았습니다. 국내 레퍼런스는 **「공공기관·대기업」** 으로만 표현합니다.
- **청와대는 맞춤 제안서 화면·PDF·메일 어디에도 언급하지 않습니다**(기본값에서 제외). `PROPOSAL_BRAND_PUBLIC` 에도 넣지 마세요.

### ⑤ 영문(EN) — 사이트 전체

- 신청 화면 · 진행 화면 · 상담 폼 · 파인더 · 자료실 배너 · FAQ · 안내 문구 · 버튼 · 접근성 라벨까지 영문 사전(`i18n.js`)에 넣었습니다.
- 진행 화면은 **서버가 영문으로 내려줍니다**(`/api/proposal/status?lang=en`). 산업·과제·사례·플레이북 886개 항목 사전(`data/proposal/i18n/en.json`)을 쓰고, 사전에 없는 항목은 한글을 섞지 않고 화면에서 뺍니다.
- **제안서 PDF·메일은 한국어**로 나갑니다. 영문 화면에는 「PDF·메일은 한국어 — 영문 제안서는 문의」 안내가 붙습니다.
- 사례 문구가 바뀌면 `en.json` 에도 넣어 주세요. 빠진 항목은 빌드 로그에 `[proposal-sync] 영문 없음` 으로 나옵니다.

### ⑥ 「가장 닮은 사례 자세히 보기」 → 나갔다가 돌아오기

- 사이트 안 사례(`/case/…`)는 **같은 탭에서 화면만 바뀌고**, 화면 아래에 **「내 맞춤 제안서 진행 현황 →」(또는 「맞춤 제안서 신청으로 돌아가기」) 버튼**이 뜹니다. 누르면 보던 화면으로 바로 돌아오고, 브라우저 뒤로가기도 됩니다.
- 이 버튼은 이 탭에서 신청한 적이 있으면 사이트 어디서나(홈·제품·사례) 뜨고, ×로 닫으면 그 탭에서는 다시 안 뜹니다. 신청 내용은 탭 저장소에 남아 있어 돌아와도 그대로입니다.
- 외부 사례(해외 Monnit 사이트)는 **새 탭**으로 열려 제안서 화면이 그대로 남습니다(`noopener noreferrer`).

### ⑦ 보안 (연동은 그대로 동작하도록)

| 항목 | 내용 |
|---|---|
| 중복 신청 응답 | 전에는 같은 회사·이메일로 다시 신청하면 **기존 진행 화면 토큰을 응답에 돌려줘서**, 남의 이메일·회사명을 아는 사람이 진행 화면을 볼 수 있었습니다 → **토큰을 주지 않고, 신청한 메일로 링크를 다시 보냅니다**(10분에 한 번) |
| 요청 출처 | `Origin` + `Sec-Fetch-Site` 확인. 허용: 사이트 주소 · monnit.co.kr · 배포 미리보기 주소(`URL`/`DEPLOY_URL`/`DEPLOY_PRIME_URL`) · localhost. **다른 `*.netlify.app` 은 거절** |
| 요청 형식·크기 | JSON만, 16KB 초과 413 |
| 개인정보와 주소 | 견적 요청으로 넘어갈 때 회사·성함을 **주소에 싣지 않고** 탭 저장소로 전달(주소에는 `?quote=제안서번호`만) |
| 링크 | 화면에 그리는 사례 주소는 사이트 안 경로·https 만 허용 |
| 관리 화면 | 응답마다 **nonce 기반 CSP**(외부 스크립트·프레임·폼 전송 차단), 모든 POST 같은 출처 확인, **로그인 8회 실패 → 15분 잠금**(IP는 해시로만 저장), 로그아웃 POST 전용, COOP·Permissions-Policy |

### ⑧ 연동 점검 — 관리 화면 버튼 · 명령어

- 관리 화면 상단 **「연동 점검」**: 설정값 · 저장소 읽기·쓰기 · PDF 조판(배너·로고·글꼴) · 정기 점검 기록 · 경쟁사 차단 연결 · 관제 로그인 공유 · 먼데이 설정을 한 번에 확인합니다.
- **「외부 서비스까지 확인 (읽기 전용)」**: Brevo 계정 · Claude 모델 · 먼데이 API · 메일 로고 공개 주소 · 백그라운드 함수 호출. 메일을 보내거나 항목을 만들지 않습니다.
- 배포 전 로컬: `node tools/check-proposal.mjs` (외부 호출 포함은 `--deep`).

### ⑨ 반응형·인터랙션

- 버튼·카드·칩: 누름(살짝 작아짐) · 호버 · 키보드 초점 테두리 · 로딩(스피너) · 비활성 상태.
- 상담 폼 「상담만 / 맞춤 제안서도 받기」: 기본 라디오 대신 **카드형 선택**(캡처의 네모 안 라디오 문제 해결).
- 터치 기기는 누르기 쉬운 크기(40~48px), 아이폰 하단 안전 영역, 「동작 줄이기」 설정이면 애니메이션 끔. 390px 폭에서 가로 넘침 없음 확인.

---

## 00. v4에서 달라진 점

### ① 어떤 회사·어떤 입력이든 그 값으로 PDF와 메일이 만들어지는지 — 확인 결과

- 12개 산업 × 세부 업종 × 과제 **211개 조합**을 회사명·성함을 바꿔 가며(한글·영문 회사명, 처음 보는 회사 포함) 모두 만들어 봤습니다 → **전부 10쪽**, 표지·요약에 그 회사명·성함, 마지막 쪽에 그 건의 견적 요청 주소가 들어갔습니다.
- 메일 제목·본문·첨부 파일 이름도 그 건의 회사·성함·과제로 채워집니다(자동 테스트 통과).
- **바로(2~3분) 나가지 않는 경우** — 업종을 판단할 근거가 없거나(예: 개인 메일 + 「문의드립니다」), 고른 산업과 회사 인식이 다르거나, 규칙 점검(근거 없는 수치·약속 표현 등)에 걸리면 **엔지니어 확인 후 발송(영업일 1일 안)** 으로 넘어갑니다. 경쟁사·봇은 조용히 막고, 같은 회사·이메일이 다시 신청하면 아래 ②처럼 처리합니다.
- 실제 발송은 `BREVO_API_KEY` 와 발신 도메인 인증이 끝난 배포에서만 됩니다(아래 9장 체크리스트).

### ② 무료 제안서 = 가장 고민되는 과제 1개 · 전체는 견적 요청으로

| 위치 | 바뀐 점 |
|---|---|
| `/proposal` 2단계 | 「가장 고민되는 문제 — **하나만** 골라 주세요」(라디오형). 아래에 「여러 과제·현장 전체 구성·수량·견적은 견적 요청으로」 안내 |
| 홈 파인더 | 고민 하나로 바로 신청(원래 1개 선택) |
| 상담 폼 「제안서도 받기」 | 관심 주제 칩 **1개만** 선택. 여러 과제는 문의 내용에 적으면 상담에서 함께 봄 |
| 서버 | 여러 개가 와도 첫 번째만 제안서 과제로 쓰고, 나머지·문의 글에서 찾은 과제는 **「추가 관심 과제」로 담당자에게만** 전달(`PROPOSAL_MAX_PROBLEMS`, 기본 1) |
| PDF | 선택 과제 진단 1건 · **연결 구역만 자세히**, 나머지 구역은 이름만(「전체 현장 견적 요청 시 정리」) · 로드맵 L1~L2만 자세히, L3~L5는 제목만 · 「한 단계 더」 목록 대신 범위 안내 상자 · 마지막 쪽 CTA가 **「현장 전체 견적 요청」**(`monnit.co.kr/contact?quote=제안번호`) |
| 고객 메일 | 「이 제안서의 범위」 상자 + 견적 요청 링크. 후속 안내 메일에도 견적 요청 링크 |
| 진행 화면 | 「이 제안서는 「○○」 기준입니다」 카드(담긴 것 / 견적 요청 시 함께 정리) · 발송 뒤 하단 버튼이 **현장 전체 견적 요청** |
| 견적 요청 연결 | `/contact?quote=…` 로 오면 상담 폼에 회사·성함·문의 항목(견적)·요청 문구가 채워지고, 「상담만」으로 접수됩니다(제안 번호가 문의 내용에 들어가 담당자가 바로 찾음) |
| 재신청 | 같은 회사·이메일은 **30일에 한 번**(`PROPOSAL_ONE_PER_DAYS`). 다른 과제로 다시 오면 새로 만들지 않고 「이미 받으신 제안서가 있어요 → 견적 요청으로」 안내 + 담당자에게 **「추가 제안 요청 · 견적 연결·연락 필요」** 알림(견적 의향이 높은 신호) |

### ③ 관제에서 탭으로 들어가고 나오기

- `/ops`(통합 관제) · `/ops/proposals`(맞춤 제안서) · `/ops/proposals/archive`(발송 대장) · `/ops/proposals/insights`(고객 인사이트) · `/ops/block`(차단 관리) 위에 **같은 탭 줄**이 있습니다. 맞춤 제안서의 세 탭은 새로고침 없이 바뀌고(키보드 1·2·3), 뒤로가기도 됩니다.
- **로그인 하나로** — `/ops` 에 로그인했으면 맞춤 제안서 화면도 바로 열립니다(`_ops_auth.mjs` 를 함께 씀). 관리 키(`PROPOSAL_ADMIN_KEY`) 로그인도 계속 됩니다.
- `/ops` 화면의 탭 줄은 `netlify/functions/_ops_ui.mjs`, 차단 관리는 `blockadmin.mjs` 에 들어 있습니다.
- ⚠️ `ops.mjs` 가 `/ops/*` 처럼 넓은 경로를 잡고 있다면 `/ops/proposals*` 가 가려지지 않는지 배포 후 한 번 확인해 주세요(받은 사본에는 `ops.mjs` 가 없어 확인하지 못했습니다).

### ④ 발송 대장(아카이브) — 누구에게 무슨 자료가 나갔는지

- 고객에게 메일이 나갈 때마다(제안서 · 재발송 · 후속 안내) **한 줄씩** 남깁니다: 발송 시각 · 제안 번호 · 받는 분(회사·성함·직함·이메일·전화) · 자료(PDF 쪽수·파일 지문) · 기준 과제 · 추가 관심 · 업종·세부 업종 · 규모·시점 · 입구·채널 · 등급 · 발송 방식 · AI/템플릿·버전·검토 결과 · 발송 주체(자동/관리자) · 유사 사례.
- **그때 보낸 PDF 사본**을 그대로 보관합니다(재생성해도 이전에 나간 파일이 남음).
- 발송 대장 탭: 기간·종류 필터, 회사·이메일·과제 검색, 반응(열람·첫 열람 시각·확인 연락·추가 요청) 함께 표시, **대장 CSV**.
- **먼데이 연동(선택)** — `PROPOSAL_MONDAY_TOKEN`(없으면 관제의 `MONDAY_TOKEN`) + `PROPOSAL_MONDAY_BOARD` 를 넣으면 발송 1건마다 「제안서 발송 대장」 보드에 항목이 생기고, 열람·재방문·추가 요청·확인 연락이 그 항목의 업데이트로 쌓입니다. 열 연결은 `PROPOSAL_MONDAY_COLUMNS`(JSON).
  - 권장 구성: **기준 기록은 사이트 안 대장**(PDF 사본·반응까지 한곳), **영업 진행은 먼데이**(담당자 배정·단계 관리). 분석을 더 깊게 하려면 대장 CSV를 구글 시트/BI에 주기적으로 붙이는 방식이 가장 가볍습니다.
- 개인정보: 보관 기간(기본 12개월)이 지나면 대장에서 **받는 분 정보와 PDF 사본을 지우고** 회사·업종·과제·반응만 남깁니다(동의 문구의 「1년 뒤 파기」와 맞춤).

### ⑤ 고객 인사이트 탭

- 퍼널(접수 → 발송 → 열람 → 확인 연락 → 추가 요청) · 첫 열람까지 걸린 시간(중앙값) · 나간 자료 수
- 산업별 · 기준 과제별 · 세부 업종별 · 입구별 · 채널별 · 등급별 · 규모별 · 도입 시점별 — 접수·발송·열람률·추가 요청·평균 점수
- **함께 관심**(기준 과제 → 추가로 말한 과제): 견적 상담 때 묶어서 제안할 조합
- 신청 시간대·요일
- **먼저 연락할 회사** — 반응(열람·재방문·추가 요청)과 등급을 합친 점수, 근거를 같이 표시(연락 완료면 낮춤)
- **「Claude로 인사이트 정리」** — 위 집계(**이름·이메일·전화 제외**, 회사명·업종·과제·반응만)를 Claude에게 넘겨 「관찰 · 이번 주 할 일 · 광고·콘텐츠 · 주의」를 받습니다. 표본이 작으면 작다고 쓰게 했습니다.

### ⑥ AI 재생성 = Claude 재생성 → 확인 → 발송 여부 결정

상세 화면의 「Claude 재생성 · 검토」 상자에서:

1. **재생성** — 바라는 점을 적거나(예: 「누수 사례를 앞으로, 문장은 짧게」) 자주 쓰는 요청 칩을 눌러 시작. 새 버전(v1, v2…)을 따로 만들고, **지금 발송본은 채택 전까지 그대로**입니다.
2. **확인** — 규칙 점검(근거 없는 % 수치 · 약속·가격 표현 · 940MHz · 과제 범위 안내 · 회사 단정 표현 · 분량) + **Claude 검토**(발송 추천/수정 추천/보류 추천, 점수, 문제 목록, 다시 만들 때 넣을 요청 제안). 규칙 점검에 걸리면 Claude가 발송을 추천하지 않습니다.
3. **결정** — 이전 요약과 새 요약 비교 · 새 PDF 열어 보기 → **이 버전으로 지금 발송(재발송)** / **채택 — 예정대로 발송** / **요청 고쳐서 다시 생성** / **버리기** / **보류**.

- 버전 기록(누가 어떤 요청으로 만든 어느 버전이 발송본인지)과 각 버전 PDF가 남습니다. 발송 대장에도 버전·검토 결과가 기록됩니다.
- 자동 발송도 같은 규칙 점검을 거칩니다 — 걸리면 바로 보내지 않고 엔지니어 확인으로 넘깁니다.
- Claude 검토는 참고 의견이고, **발송은 담당자가 누른 버튼으로만** 됩니다. API 키가 없으면 템플릿으로 만들고 규칙 점검만 합니다.
- 「템플릿으로 재생성」은 그대로 있습니다(바로 교체).

---

## 0. v3에서 달라진 점

### ① 발송 시점 — 「바로 보내기」가 기본

| 방식 (`PROPOSAL_MODE`) | 고객 경험 | 언제 쓰나 |
|---|---|---|
| **`instant` (기본)** | 신청 → 진행 화면에서 실제 단계가 2~3분 진행 → **메일 도착** → 「담당 엔지니어가 영업일 기준 1일 안에 확인하고 연락드립니다」 | 평소 운영 |
| `review` | 접수 메일 → 엔지니어 확인 → 영업일 1일 안 발송 | 즉시 방식에서 **불확실한 건은 자동으로 이 방식으로 전환** |
| `delayed` | 접수 메일 → 약 2일 뒤 발송 (v2 방식) | 수동 검수를 모든 건에 하고 싶을 때 |

**왜 바로 보내나** — 신청 직후가 관심이 가장 높을 때입니다. 「신청했더니 바로 왔다」는 경험이 신뢰를 만들고, 경쟁사를 알아보기 전에 우리 자료가 먼저 읽힙니다.
2일을 기다리게 하면 그 사이 관심이 식고, 무엇을 신청했는지 잊은 상태에서 메일을 받게 됩니다.
대신 사람이 붙는 부분(확인 연락)은 영업일 기준으로 약속해, 자동 발송이 「대충 만든 자료」로 느껴지지 않게 했습니다.

- 진행 화면의 단계는 **실제로 끝난 만큼만** 나아갑니다. 즉시 방식에서는 「엔지니어 검수」 대신 **「품질 점검 — 수치·표현·구성을 자동으로 점검합니다」** 로 표시됩니다(사람이 안 본 것을 봤다고 쓰지 않음).
- 즉시 방식에서는 접수 확인 메일을 보내지 않습니다(곧 제안서가 가므로 메일 1통으로 끝). 메일 제목·본문은 「방금 정리한 맞춤 제안서입니다」.
- 발송 완료 후 진행 화면에 「확인 연락 예정」 단계가 남고, 담당자가 관리 화면에서 **「확인 연락 완료」** 를 누르면 화면에도 완료로 바뀝니다.
- 담당자 메일 제목에 `·즉시 발송` / `·확인 필요` / `·연락 필요` 표시.

**자동으로 검수 방식으로 넘기는 경우** (`netlify/lib/proposal/plan.mjs`)

- 업종을 판단하지 못함(기본값으로 떨어짐)
- 고른 산업과 회사 인식 결과가 다름
- 도메인만 사례 고객과 겹치고 이름은 확인 안 됨(그룹 공용 도메인 등)
- 회사 인식 확신도가 낮고, 산업을 직접 고르지도 않았고, 세부 업종도 없음
- 해당 산업 플레이북이 없음
- 즉시 생성이 실패하거나 5분 넘게 끝나지 않음

→ 고객에게는 「입력하신 정보만으로는 판단이 어려운 부분이 있어, 담당 엔지니어가 확인한 뒤 보내드립니다」 + 접수 메일, 담당자에게는 사유가 적힌 「확인 필요」 알림.

### ② 사이트에 자연스럽게 붙임 — 별도 페이지 → 사이트 화면

| 주소 | 화면 | 방식 |
|---|---|---|
| `/proposal` | 신청 화면 (`#view-proposal`) | 사이트 SPA 화면. 빌드 때 **검색엔진용 정적 본문**(서비스·FAQ 구조화 데이터, 산업별 요약)도 `proposal/index.html` 로 생성 |
| `/proposal/status` | 진행 현황 (`#view-proposal-status`) | SPA 화면. 정적 파일 없음 → `_redirects` 폴백 · robots 차단 · noindex |

- 사이트의 `navigate()` 에 `proposal` 경로가 추가됐습니다. 메뉴·뒤로가기·검색·언어 전환이 다른 화면과 똑같이 동작합니다.
- 화면 코드는 **처음 들어갈 때만** `js/proposal-data.js` → `js/proposal-view.js` 를 불러옵니다(홈 첫 로딩 무게 변화 없음).
- 스타일은 `style.css` 끝에 `.mkp-` / `.mkps-` 접두어로 추가(기존 `pp-`·`pps-` 클래스와 충돌 방지). 하단 신청 버튼은 쿠키 동의 바 위에 고정.
- 편집기(`/editor`)에 **「◆ 맞춤 제안서 · /proposal 상단」** 그룹이 생겨 제목·설명·지표 문구를 코드 수정 없이 바꿀 수 있습니다.

**입구**

| 위치 | 내용 |
|---|---|
| 헤더 Resources 메뉴 | `Custom Proposal · 맞춤 제안서` |
| 홈 솔루션 파인더 결과 | **「이 조건으로 맞춤 제안서 받기」** 가 주 버튼(선택한 시설·고민·규모가 그대로 넘어가 회사·연락처만 입력) · 기존 상담 버튼은 보조 |
| 홈 빠른 상담(리드 위젯) | 「전화 대신 맞춤 제안서(PDF)로 먼저 받아보기」 |
| 상담 신청 폼 (`/contact`) | 「상담만 / **맞춤 제안서도 받기**」 선택 → 관심 주제 칩·시설 입력·동의 → 제출하면 상담 접수 + 제안서 접수 → 진행 화면으로 이동. 제안서 접수가 실패하면 **평소 상담 접수로 그대로** |
| 자료실 (`/whitepaper`) | 「산업 공통 제안서 대신, 우리 회사만을 위한 제안서가 필요하다면」 배너 |
| 푸터 · 사이트 검색 | 맞춤 제안서 링크 · 검색어 「제안서」「견적」「구성안」 |

### ③ 등급 근거 — 「왜 C등급인지」 보이게

등급은 10개 항목 점수의 합입니다(`netlify/lib/proposal/grade.mjs`).

| 항목 | 점수 | 항목 | 점수 |
|---|---|---|---|
| 회사 이메일(무료 메일이면 0) | 15 | 현장 설명 입력 | 8 |
| 연락처 | 15 | 직함 | 4 |
| 도입 시점 | 20 | 회사 인식(확신도 80% 이상) | 5 |
| 감시 규모 | 15 | 작성 시간 1분 이상 | 5 |
| 과제 구체성(직접 고름) | 10 | 사례 열람 | 5 |

**A 60점 이상 · B 35점 이상 · C 그 밖**

- 담당자 메일·관리 화면에 **등급 · 근거 · 의미 · 올리려면** 이 함께 표시됩니다.
  예) `C등급 15점 — 개인 메일 사용 −15 · 도입 시점 미입력 −20 · 감시 규모 미입력 −15`
  의미: 「정보 수집 단계일 가능성이 큽니다 — 제안서 열람·재방문 같은 반응을 보고 연락」
  올리려면: 「B등급까지 20점 — 도입 시점만 확인돼도 올라갑니다」
- 관리 화면 상세에 항목별 점수표(「등급 근거」), 목록에 사유 한 줄, CSV에 `score`·`gradeWhy` 열.
- 등급 의미: **A** 도입 검토가 구체적 — 당일 전화 / **B** 관심 확인 — 발송 뒤 1~2일 안 연락 / **C** 정보 수집 단계 — 열람·재방문 반응을 보고 연락.

---

## 1. v2 기능 (그대로 유지)

### 회사 인식 (`netlify/lib/proposal/company.mjs`)

순서: ① 알려진 기업 목록(이름·별칭·이메일 도메인, 기업·기관 이름·별칭 약 300개) ② 도메인 규칙(`.go.kr` `.mil.kr` `.ac.kr` 학교·병원 도메인) ③ 업종 키워드(회사명 → 시설·문의 글) ④ 폼 산업군 선택값.

- 예: `삼성바이오로직스` → 제약·바이오 › **바이오의약품 생산·CDMO** (확신도 95%, 도메인까지 맞으면 98%)
- 모넷 사례집에 있는 고객(삼성바이오로직스·현대자동차·현대건설 등)은 **「기존 도입 고객 → 확장 제안」**으로 구성합니다. 자기 사례는 비교 순위에서 빼고 별도 박스로 보여줍니다.
- 약칭 오인식 방지: `LX판토스`는 LX가 아님, `대상정밀`은 식품 대상이 아님(짧은 이름은 정확히 같을 때만).
- 무료 메일(gmail·naver 등)은 도메인으로 아무것도 추정하지 않습니다.
- 확신도가 **0.8 미만이면 제안서에서 회사 이름을 들어 단정하지 않고** 「입력하신 정보를 바탕으로 ○○ 현장 기준」으로 씁니다.
- 신청 화면에서 회사명을 치면 `🔎 바이오의약품 생산·CDMO 현장 기준으로 제안서를 구성합니다` 칩이 뜹니다. 고른 산업과 확실히 다르면 「산업을 ○○로 바꾸기」 버튼을 줍니다.
- 기업 목록 추가: `company.mjs`의 `DIRECTORY`에 한 줄 (`n` 이름·별칭, `d` 도메인, `i` 산업, `s` 세부 업종, `c` 사례 키).

### 산업 플레이북 (`data/proposal/playbooks/*.json`, 12개 산업 · 세부 업종 46개)

산업마다 현장 엔지니어가 읽어도 수준 있게 쓴 본문입니다. 빌드 때 `scripts/proposal-sync.mjs`가 검사(센서·과제 키, 금지 표현)한 뒤 `netlify/lib/proposal/playbooks.data.mjs`로 만듭니다.

| 항목 | 내용 |
|---|---|
| `context` | 이 산업에서 설비·환경 모니터링이 왜 중요한지 |
| `chronic` (4) | **산업의 고질적인 문제** — 왜 반복되는지, 놓치면 생기는 일 |
| `personas` (3~4) | **담당자별 어려움**과 그중 센서·알림으로 대처 가능한 부분 |
| `zones` (5~7) | **공정·구역별 관리 포인트 · 모넷 센서 · 있으면 좋은 것** |
| `niceToHave` (4) | 한 단계 더 나아간 모니터링 |
| `automation` (5) | **스마트 관리 로드맵** L1 가시화 → L2 알림·대응 체계 → L3 시스템 연동 → L4 자동 제어 → L5 예측·최적화 |
| `kpis` · `compliance` · `consult` | 추적 지표 · 규정 대응 포인트 · 현장 진단 질문 |
| `segments` | 세부 업종별 추가 문제·구역 + 인식 키워드 (예: bio_pharma → biologics / pharma / lab / gdp) |

조립 규칙(`match.mjs` `playbook()`): 세부 업종 내용을 앞에, **고객이 고른 과제와 연결된 문제·구역을 강조(파란색)**, 비슷한 항목은 합치고 구역표는 7행 이내.
문구를 고치려면 JSON 파일만 수정하고 배포하면 됩니다(숫자·가격·「보장」 같은 표현은 빌드 로그에 경고).

### 제안서 PDF v2 (약 10쪽)

1 표지(세부 업종·확장 제안 배지) · 2 제안 요약 + **귀사 현장 이해** · 3 **산업의 고질적인 문제** · 4 **담당자별 어려움** · 5 **공정·구역별 모니터링 맵** · 6 선택 과제 진단 · 7 유사 사례(+귀사 도입 사례) · 8 권장 구성 · 9 **스마트 관리 로드맵 + 운영 지표** · 10 **현장 진단 체크리스트 · 규정 대응 · 다음 단계**

- 12개 산업 × 세부 업종 58개 조합 모두 9~11쪽으로 조판되는 것을 확인했습니다.
- Claude 문안(키가 있을 때)은 `귀사 현장 이해`·`구역 시작 순서`·`로드맵 도입 문장`까지 씁니다. 자료에 없는 회사 사실(매출·공장 수 등) 추측 금지, 센서로 못 하는 일 금지 규칙이 추가됐습니다.
- v2 이전에 접수된 건은 재생성 시 매칭을 다시 계산해 v2로 만듭니다.

### 담당자 알림·관리 화면

신규 접수 메일과 관리 화면 상세에 **「자동 판단」** 블록이 붙습니다 — 입구 · 업종 판단(근거) · 회사 인식 확신도와 이유 · 과제 판단 방식 · 기존 고객 여부 · 확인 필요 사항(예: 고른 산업과 회사 인식이 다름). 목록에는 입구(파인더/상담폼)와 「기존 고객」 표시가 붙습니다.

### 새 API

- `GET /api/proposal/detect?company=&email=&fac=&con=` — 회사 인식 미리보기 (고객 목록 여부는 내려보내지 않음)
- `GET /api/proposal/preview` — `segment` 인자 추가, 응답에 플레이북 요약 포함
- `POST /api/proposal` — `entry`(finder·contact·widget·proposal), `auto`, `fac`·`con`·`scale`, `industryText`, `inquiry`, `concerns[]` 추가. 자동 입구는 산업·과제를 고르지 않아도 접수됩니다(동의는 필수).

---

## 2. 흐름 (즉시 방식)

```
[고객] 입구(파인더·상담폼·빠른 상담·메뉴…) → /proposal 신청 화면 (사이트 안)
        │ POST /api/proposal
        ▼
[서버] 검증 → 경쟁사 차단 → 봇·중복·횟수 제한 → 발송 방식 결정(decidePlan) → 저장
        ├─ 담당자: 신규 접수 알림 (등급·근거·「즉시 발송」)
        ├─ 사이트 리드 원장(/api/lead) 기록 → 관제·먼데이
        └─ 백그라운드: 문안 + PDF 조판 → 예정 시각(약 2분 30초 뒤)까지 기다렸다가 발송
        ▼
[고객] /proposal/status  ← 발송 전까지 3초마다 갱신, 남은 시간 mm:ss
  고객 정보 수집 → 시설·과제 분석 → 모넷 데이터 취합 → 인사이트 알고리즘 가동
  → 맞춤 제안서 작성 → 품질 점검 → 제안서 발송 → (확인 연락 예정)
        ▼
  · 고객: PDF 첨부 + 30일 링크 메일 「방금 정리한 맞춤 제안서입니다」
  · 담당자: 「발송 완료·연락 필요」 → 연락 후 관리 화면 「확인 연락 완료」
  · 고객이 PDF를 처음 열면: 담당자에게 「제안서 열람」 알림 ← 전화하기 좋은 때
  · 3일 뒤에도 안 열었으면: 후속 안내 메일 1회
  · 안전장치: 진행 화면 요청 · 10분 점검(proposal-dispatch)이 늦은 건을 이어서 처리
  · 1년 지나면: 개인정보 파기
```

검수·지연 방식은 v2와 같습니다(접수 메일 → 발송 20시간 전 담당자 검수 알림(지연 방식) → 예정 시각 발송).
어떤 방식이든 **실제로 발송되기 전에는 「발송 완료」가 뜨지 않습니다.**

---

## 3. 추가·변경 파일

### 사이트 (기존 파일 수정)

| 파일 | 변경 |
|---|---|
| `index.html` | `#view-proposal` · `#view-proposal-status` 화면 추가, 메뉴·파인더·빠른 상담·상담 폼·자료실·푸터 입구, 토큰 정리 스크립트(head), `style.css?v=121` · `app.js?v=127` · `i18n.js?v=114` (화면 스크립트 `?v=5`) · **v5** 홈 사실 칸·FAQ·문구 |
| `app.js` | **v5** 「내 맞춤 제안서」 돌아가기 버튼(`proposalPill`) · 견적 요청 채우기를 탭 저장소 방식으로 · **v4** `/contact?quote=` 견적 요청 채우기(`applyContactQuote`) · 상담 폼 관심 칩 1개 · `navigate('proposal' / 'proposal/status')`, 화면 스크립트 지연 로딩(`ensureProposal`), 주소 인자 정리, 상담 폼 「제안서도 받기」 처리(`contactProposal`), 편집기 슬롯 대상에 `view-proposal` 추가 |
| `style.css` | 끝에 「맞춤 제안서 (2026-09-17)」 블록 (`.mkp` `.mkps` · 입구 스타일 · 반응형) · **v5** 블록(돌아가기 버튼 · 인터랙션 · 카드형 라디오) |
| `i18n.js` | 새 문구 영문 · **v5** 맞춤 제안서 전체 영문(약 110개) · `data-i18n-html` 문단 · aria-label 번역 |
| `editor.html` | 「맞춤 제안서」 편집 그룹 |
| `netlify/functions/_ops_ui.mjs` · `blockadmin.mjs` | **v4** 관제 탭 줄(통합 관제 · 맞춤 제안서 · 발송 대장 · 고객 인사이트 · 차단 관리) |
| `netlify/functions/proposal-admin.mjs` | **v4** 탭 3개(접수·발송 / 발송 대장 / 고객 인사이트) · Claude 재생성 흐름 · 관제 로그인 공유 |
| `build.js` | **v5** OG 이미지(`image`) 지원 · 글로벌 문구·FAQ · `/proposal` 정적 본문 생성(`writePage`), sitemap 자동 포함, `_redirects` 폴백·옛 주소 301, robots `/proposal/status` 차단 |
| `_redirects` | 위 폴백 4줄 (빌드 때 자동 관리 블록 — 배포 시 다시 만들어짐) · `/assets/fonts/*` 404 |
| `netlify.toml` | 빌드 명령에 `proposal-sync`, `/proposal/status` 헤더(noindex·no-referrer·no-cache), 스크립트 캐시 헤더, 함수별 포함 파일 |
| `package.json` · `package-lock.json` | `pdf-lib`, `fontkit` |

### 새 파일

| 파일 | 내용 |
|---|---|
| `js/proposal-view.js` | 신청·진행 현황 화면 동작 (`window.MKProposal`) — 빠른 신청, 회사 인식 칩, 임시 저장, 진행 막대·카운트다운·알림 연출, 시연 모드, 공유 |
| `js/proposal-data.js` | **자동 생성** — 산업·문제표, 사례 요약, 파인더 코드, 플레이북 요약 |
| `netlify/functions/proposal-api.mjs` | `/api/proposal` 접수 · `/status` · `/pdf` · `/preview` · `/detect` |
| `netlify/functions/proposal-build-background.mjs` | 생성 (+ 즉시 방식은 예정 시각에 발송) |
| `netlify/functions/proposal-dispatch.mjs` | 10분 점검 — 발송·재시도·검수 알림·후속·파기·지연 건 전환 |
| `netlify/functions/proposal-admin.mjs` | 관리 화면 `/ops/proposals` |
| `netlify/lib/proposal/*.mjs` | 설정·저장소·일정·매칭·문안·PDF·메일·파이프라인·회사 인식·접수 해석 · **v3 `plan.mjs`(발송 방식) · `grade.mjs`(등급 근거)** |
| `netlify/lib/proposal/cases.data.mjs` · `playbooks.data.mjs` | **자동 생성** |
| `data/proposal/playbooks/*.json` | 산업별 플레이북 원본 12개 |
| `scripts/proposal-sync.mjs` | 빌드 단계 — 시트·플레이북 → 자동 생성 파일 |
| `assets/fonts/Pretendard-*.ttf` | PDF용 한글 글꼴 (공개 주소는 404) |
| `tools/proposal-dev.mjs` | 로컬 개발 서버 (Netlify와 같은 주소 규칙) |
| `netlify/lib/proposal/archive.mjs` | **v4** 발송 대장 · PDF 사본 · 익명화 · 인사이트 집계 · Claude 인사이트 |
| `netlify/lib/proposal/crm.mjs` | **v4** 먼데이 발송 대장 연동(설정 있을 때만) |
| `netlify/lib/proposal/review.mjs` | **v4** 규칙 점검 · Claude 검토 |
| `netlify/lib/proposal/opsauth.mjs` | **v4** 통합 관제 로그인 공유 |
| `tools/test-proposal.mjs` | 전 구간 자동 테스트 **155개 항목** |
| `맞춤제안서-이전파일-삭제.sh` | v2 별도 페이지 파일 정리 |
| `netlify/lib/proposal/i18n.mjs` | **v5** 진행 화면 영문 변환 |
| `netlify/lib/proposal/health.mjs` · `tools/check-proposal.mjs` | **v5** 연동 점검 |
| `netlify/lib/proposal/brand.data.mjs` · `en.data.mjs` | **v5 자동 생성** — 배너·로고(base64) · 영문 사전 |
| `data/proposal/brand/` | **v5** 배너·로고 원본 (PDF용) |
| `data/proposal/i18n/en.json` | **v5** 산업·과제·사례·플레이북 영문 사전 |
| `assets/brand/` | **v5** 메일·사이트용 로고(WebP·PNG) · 배너 · OG 이미지 |

### 지울 파일 (v2를 올렸던 경우)

```
proposal.html  proposal-status.html
js/proposal-apply.js  js/proposal-status.js  js/proposal-bridge.js
css/proposal.css
```

`sh 맞춤제안서-이전파일-삭제.sh` 로 한 번에 지울 수 있습니다. 옛 주소(`/proposal.html`, `/proposal-status.html`)는 새 주소로 301 됩니다.
기존 함수 중에서는 관제 탭 줄을 넣기 위해 `_ops_ui.mjs`(화면 문자열)와 `blockadmin.mjs`(화면 머리) 두 곳만 고쳤습니다. 동작 코드는 그대로입니다.

---

## 4. 환경변수 (Netlify → Site configuration → Environment variables)

| 변수 | 필수 | 설명 |
|---|---|---|
| `BREVO_API_KEY` | ● | 고객 메일(첨부) 발송. 이미 쓰는 값 그대로 |
| `PROPOSAL_SECRET` | ● | 진행 화면·PDF 링크 서명 키(긴 임의 문자열) |
| `PROPOSAL_ADMIN_KEY` | ● | 관리 화면 로그인 키 |
| `PROPOSAL_STAFF_TO` | 권장 | 담당자 알림 주소(쉼표 구분). 기본 `NOTIFY_TO` → `korea@monnit.com` |
| **`PROPOSAL_MODE`** | 선택 | **v3** `instant`(기본) · `review` · `delayed` |
| **`PROPOSAL_INSTANT_SECONDS`** | 선택 | **v3** 즉시 방식에서 신청 → 발송까지 초. 기본 `150` (5~600) |
| **`PROPOSAL_REVIEW_HOURS`** | 선택 | **v3** 검수 방식 발송까지 시간(영업시간 보정 — 밤·주말 접수는 다음 영업일 오전). 기본 `4` (「몇 시간 이내」 안내와 맞춤) |
| **`PROPOSAL_CALLBACK_TEXT`** | 선택 | **v3** 확인 연락 약속 문구. 기본 `영업일 기준 1일 안에` |
| **`PROPOSAL_MAX_PROBLEMS`** | 선택 | **v4** 무료 제안서가 다루는 과제 수. 기본 `1` |
| **`PROPOSAL_ONE_PER_DAYS`** | 선택 | **v4** 같은 회사·이메일 무료 제안서 간격(일). 기본 `30` |
| **`PROPOSAL_MONDAY_TOKEN`** | 선택 | **v4** 먼데이 API 토큰(없으면 관제의 `MONDAY_TOKEN`) |
| **`PROPOSAL_MONDAY_BOARD`** · `PROPOSAL_MONDAY_GROUP` | 선택 | **v4** 「제안서 발송 대장」 보드 ID · 그룹 ID |
| **`PROPOSAL_MONDAY_COLUMNS`** | 선택 | **v4** 열 연결 JSON 예) `{"company":"text","email":"email","phone":"phone","industry":"text1","problem":"text2","grade":"status","sent":"date4","no":"text3"}` |
| `ANTHROPIC_API_KEY` | 선택 | 있으면 Claude가 문안 작성 · 재생성 검토 · 인사이트 정리, 없으면 템플릿 + 규칙 점검 |
| `PROPOSAL_AI_MODEL` | 선택 | 기본 `claude-sonnet-4-5` |
| `PROPOSAL_DELAY_HOURS` | 선택 | 지연 방식 발송까지 시간. 기본 `48` |
| `PROPOSAL_BUSINESS_HOURS` · `PROPOSAL_SEND_HOUR` · `PROPOSAL_HOLIDAYS` | 선택 | 검수·지연 방식의 영업일 보정 (기본 켬 · 10시 · 추가 휴일) |
| `PROPOSAL_REVIEW` | 선택 | `auto`(기본) / `manual` = 승인한 건만 발송 (검수·지연 방식에 적용) |
| `PROPOSAL_PREVIEW_HOURS` | 선택 | 지연 방식 검수 알림 시점. 기본 `20` |
| `PROPOSAL_FOLLOWUP_DAYS` | 선택 | 미열람 후속 메일. 기본 `3`, `0`이면 끔 |
| `PROPOSAL_WEBHOOK_URL` | 선택 | 알림을 슬랙·팀즈 등으로도 전송 |
| `PROPOSAL_RATE_IP_HOUR` / `PROPOSAL_RATE_DAY` | 선택 | 접수 상한 (기본 5 / 200) |
| `PROPOSAL_RETAIN_DAYS` | 선택 | 개인정보 보관 기간. 기본 `365` |
| `PROPOSAL_FROM_EMAIL` | 선택 | 발신 주소. 기본 `no-reply@monnit.co.kr` |
| **`PROPOSAL_ETA_TEXT`** | 선택 | **v5** 고객 화면 도착 안내. 기본 `몇 시간 이내` |
| **`PROPOSAL_CHAT`** | 선택 | **v7** `on`(기본) / `off` = 「대화로 신청」 탭 숨김 |
| **`PROPOSAL_CHAT_MODEL`** | 선택 | **v7** 대화에 쓰는 모델. 기본 `claude-haiku-4-5` (가장 싼 모델) |
| **`PROPOSAL_AI_BUDGET_USD`** | 선택 | **v7** 대화 AI 한 달 한도(달러). 기본 `20`, `0` = 한도 없음 |
| **`PROPOSAL_AI_PAUSE_PCT`** | 선택 | **v7** 한도의 몇 %에서 「점검 중」으로 전환할지. 기본 `95` |
| **`PROPOSAL_CHAT_IP_DAY`** · **`PROPOSAL_CHAT_MAX_TURNS`** | 선택 | **v7** 한 사람 하루 AI 호출 상한 `40` · 한 대화 최대 턴 `24` |
| **`PROPOSAL_BRAND_COUNTRIES`** · **`PROPOSAL_BRAND_CUSTOMERS`** · **`PROPOSAL_BRAND_PUBLIC`** | 선택 | **v5** 글로벌 문구 숫자·국내 대표 레퍼런스. 기본 `130+` · `64,000+` · `공공기관·대기업` |

---

## 4-B. v7 — 「대화로 신청」 (제안서 챗봇)

`/proposal` 위쪽에 탭 두 개가 생겼습니다. **단계별 신청**(기존 그대로) · **대화로 신청**(새로).

```
대화로 신청
  「안녕하세요. 회사명을 알려 주세요」   ← 코드가 묻는다(비용 0)
   → 성함 → 이메일 → 현장(버튼 10개) → 가장 큰 고민(버튼 10개)
   → 확인 카드(입력값 그대로 + 개인정보 동의) → [제안서 신청]
   → 기존 /api/proposal 로 접수 → 진행 화면(MK-P…) → 같은 PDF 자동 발송
```

**핵심은 「기본은 코드, 못 알아들을 때만 AI」입니다.**

| 들어온 말 | 처리 | 비용 |
|---|---|---|
| 회사명·성함·이메일·현장·고민 (사전에 있는 낱말·버튼 선택·수정·확인) | **코드(규칙)** 가 바로 알아듣고 다음 질문 | **0원** |
| 규칙이 못 알아들은 문장·되묻는 질문 | **Claude 가장 싼 모델**(기본 `claude-haiku-4-5`) 1회 | 대화 1회 ≈ $0.001 안팎 |
| 가격·견적·납기·A/S·채용 등 제안서 밖의 요청 | AI 없이 **담당자 연결** 안내 + 리드 기록(접점 `대화로 신청`) | 0원 |
| 경쟁사·봇 | 차단한 티를 내지 않고 일반 안내만 | 0원 |

**돈이 나가는 곳** — claude.ai Max 구독과 **별개로** Console API 키(`ANTHROPIC_API_KEY`) 사용량으로 청구됩니다. 그래서 홈페이지가 스스로 한도를 지킵니다.

- `PROPOSAL_AI_BUDGET_USD` (기본 `20`) — 이번 달 한도(달러). `0` 이면 한도 없음
- `PROPOSAL_AI_PAUSE_PCT` (기본 `95`) — 한도의 이 비율에 닿으면 **AI 호출을 멈추고** 화면에 **「대화 기능 점검 중 — 아래 단계별 신청으로 접수해 주세요」** 를 띄웁니다. 접수 자체는 계속 됩니다
- `PROPOSAL_CHAT_IP_DAY` (기본 `40`) · `PROPOSAL_CHAT_MAX_TURNS` (기본 `24`) — 한 사람이 한도를 다 쓰지 못하게 막는 상한
- 사용량은 `ai/usage-YYYY-MM.json` 에 쌓이고, **관제 「연동 점검」** 에 `대화 AI 이번 달 사용액 $x.xx / $20 (n%) · n회` 로 보입니다

**안전장치**

- 답변에서 `보장·최저가·무조건·900MHz·청와대·경쟁사명·금액` 은 걸러 냅니다(그대로 내보내지 않음)
- 대화가 24턴을 넘으면 단계별 신청으로 안내합니다
- 대화 서버가 죽어도 화면이 멈추지 않고 **「단계별 신청으로」** 버튼이 나옵니다
- 새로고침해도 대화·입력값이 유지됩니다(브라우저 안에만 저장)
- 접수 경로는 기존과 완전히 동일 — PDF·메일·발송 대장·먼데이·후속 메일이 그대로 돕니다(`entry: chat`)

**끄고 싶으면** `PROPOSAL_CHAT=off` — 탭이 사라지고 단계별 신청만 남습니다.

---

## 5. 운영

**관리 화면** `https://monnit.co.kr/ops` 로그인 → 탭 「맞춤 제안서 · 발송 대장 · 고객 인사이트」 (따로 들어올 때는 `/ops/proposals` + `PROPOSAL_ADMIN_KEY`)

- 목록: 접수 시각 · 상태 · **방식** · 등급(+근거 한 줄) · 회사 · 1위 사례 · 발송 예정 · 열람 수 · 「연락 필요」「확인 필요」 표시
- 한 건 열기: **Claude 재생성 · 검토 상자 · 발송 이력(보낸 PDF)** · **등급 근거표 · 확인 연락 완료** · PDF 미리보기 · 보류/해제 · 승인 · 지금 발송 · 재발송 · 재생성 · 취소 · 발송 시각 변경 · 요약 수정 · 메모 · 후속 메일 끄기 · 고객 화면 보기
- 상단: 현재 방식, 연락 대기 건수, 이번 달 접수·발송·열람률·A등급, CSV, 점검 실행

**권장 운영 루틴 (즉시 방식)**
1. 「발송 완료·연락 필요」 메일 → A등급은 당일, B등급은 1~2일 안 전화 → **확인 연락 완료** 누르기
2. 「확인 필요」 메일 → 사유 확인 → PDF 미리보기 → 필요하면 요약 수정·재생성 → 승인/지금 발송
3. 「제안서 열람」 메일 → C등급도 이때 연락 (가장 반응이 좋은 시점)
4. 「추가 제안 요청 · 견적 연결」 메일 → 전체 현장 견적 상담으로 연결(견적 의향이 가장 높은 신호) → 확인 연락 완료
5. 주 1회 「고객 인사이트」 → 먼저 연락할 회사 · Claude로 인사이트 정리

**사례 데이터 보강** — 구글 시트 Cases 탭에 추가하면 다음 배포 때 반영. 새 사례 산업은 `kb.mjs`의 `CASE_INDUSTRY`에 한 줄.

---

## 6. 장애·예외 상황별 동작

| 상황 | 동작 |
|---|---|
| Blobs 저장 실패 | 담당자에게 접수 내용을 메일로 보내고, 고객에게는 「담당자가 직접 보내드립니다」 화면 |
| 백그라운드 생성 호출 실패 | 정기 점검이 3분 뒤부터 다시 깨움 (최대 3회) → 그래도 실패면 담당자 알림 |
| Claude API 지연·오류·이상한 답 | 템플릿 문안으로 생성. AI 문안에서 **근거 없는 % 수치·가격·「보장」 문장은 자동 삭제** |
| AI 문안이 PDF 조판을 깨뜨림 | 템플릿으로 한 번 더 조판 |
| 발송 시각인데 아직 PDF 없음 | 그 자리에서 템플릿으로 즉시 생성 후 발송 |
| Brevo 발송 실패 | 10→20→40→80분 백오프 재시도, 5회 실패 시 자동 중단 + 담당자 알림 |
| 첨부 때문에 거절 | 첨부 없이 링크만으로 즉시 재발송 |
| 발송 도중 함수가 끊김 | **자동 재발송하지 않음** (중복 발송 방지) → 보류로 바꾸고 담당자 알림 |
| 동시에 두 곳에서 발송 시도 | 발송 잠금으로 한 곳만 진행 |
| 담당자 알림 Brevo 실패 | 사이트 기존 경로(StaticForms → Web3Forms)로 전달, 웹훅은 별도 |
| 같은 이메일+회사 24시간 내 재신청 | 새로 만들지 않고 기존 진행 화면으로 안내 |
| 봇(숨은 칸 입력·3초 안 제출) | 성공처럼 응답하고 아무것도 만들지 않음 |
| 경쟁사(데키스트 등 `_guard` 규칙) | 성공처럼 응답하고 아무것도 만들지 않음, 차단 기록은 기존 `/ops/block`에 |
| 공격성 대량 접수 | IP당 시간당 5건, 하루 200건 상한 → 전화 안내 |
| 구글 시트 접근 불가(빌드 시) | 저장소 스냅샷(`cases.data.mjs`)으로 빌드 계속 |
| 주말·공휴일·야간 | **즉시 방식은 그대로 바로 발송**(확인 연락만 영업일 기준). 검수·지연 방식은 다음 영업일 10시로 (예: 9/22 오후 접수 → 추석 연휴 → 9/28 월 10시) |
| 링크 만료·위조 | 410 + 전화 안내. 차단 IP는 PDF 링크를 받아도 열리지 않음 |
| 보관 기간 경과 | 이름·이메일·전화·메모·토큰·PDF 삭제, 통계용 산업·과제만 남김 |
| 회사 인식이 불확실 | 산업 일반 문장으로 작성. 즉시 방식이면 **검수 방식으로 자동 전환**(아래 0장) |
| 도메인만 사례 고객과 일치(그룹 공용 도메인) | 기존 고객으로 단정하지 않고 담당자에게 「확인 필요」 |
| 상담 폼에서 제안서 접수 실패 | 기존 상담 접수로 그대로 넘어감(고객에게는 평소 완료 안내) |
| 즉시 방식인데 생성 실패·지연 | 검수 방식으로 전환 → 고객 접수 메일 + 담당자 「확인 필요」 알림. 진행 화면을 보고 있으면 화면 요청이 생성을 한 번 더 깨움 |
| 즉시 방식인데 발송 시각이 지났는데 안 나감 | 진행 화면 요청·정기 점검이 이어서 발송 (중복 발송 잠금 유지) |
| 플레이북 JSON 오류 | 빌드 로그 경고 후 그 항목만 제외, 플레이북이 없는 산업은 v1(6쪽) 구성으로 발송 |
| 쿠키 동의 바가 떠 있을 때 | 하단 신청 버튼이 동의 바 위로 올라가 가려지지 않음 |

---

## 7. 보안·개인정보

- 진행 화면 주소는 24자 무작위 토큰(추측 불가), 검색 차단 + `Referrer-Policy: no-referrer`
- 사이트 안에서 열 때는 토큰을 **주소창에 남기지 않습니다** — 메일 링크(`/proposal/status?t=…`)로 들어와도 페이지가 뜨기 전에 탭 저장소로 옮기고 주소에서 지웁니다(분석 도구·공유 주소에 토큰이 남지 않음). 다른 메뉴로 이동하면 제안서 관련 주소 인자도 정리됩니다
- 진행 화면 응답에는 **전화번호·이메일 원문이 없습니다** (이메일은 `ja**@…`로 가림)
- PDF는 공개 주소가 없고, HMAC 서명 링크(30일)로만 열림
- 관리 화면: HttpOnly·Secure·SameSite=Strict 쿠키, 작업 요청은 CSRF 헤더 + 같은 출처 확인, nonce CSP, 로그인 8회 실패 시 15분 잠금
- 중복 신청에는 진행 화면 토큰을 돌려주지 않고 신청한 메일로 링크를 다시 보냄 · 요청 출처·형식·크기 제한 (자세히는 000-⑦)
- 수집 동의 문구(필수)·마케팅 수신(선택) 분리, 보관 1년 후 자동 파기
- 고객 메모는 AI에 **자료로만** 전달(지시문 무시 규칙 포함)

---

## 8. 마케팅 활용

**입구 GTM 이벤트** — 파인더 버튼 `proposal_entry_click`(`from=finder`, `fac`, `con`), 접수 성공 `proposal_submit` 에 `proposal_entry`(finder · contact · proposal) 포함

**산업별 광고 URL** — 들어오자마자 산업·문제가 선택된 상태로 열립니다.

```
/proposal?industry=datacenter&problems=leak,hotspot&utm_source=meta&utm_campaign=custom_proposal&utm_content=idc_leak
/proposal?industry=cold_chain&problems=cold_storage,record&utm_source=naver&utm_content=haccp
/proposal?industry=manufacturing&problems=downtime,wiring&utm_source=google&utm_content=pdm
/proposal?industry=residential&problems=leak,freeze&utm_source=kakao&utm_content=winter
```

산업 키: `manufacturing` `bio_pharma` `datacenter` `building_fm` `energy` `cold_chain` `public` `edu_med` `food_agri` `residential`
문제 키: `kb.mjs`의 `PROBLEMS` 참고

**GTM(dataLayer) 이벤트** — GA4 전환·리마케팅 잠재고객으로 바로 쓸 수 있습니다.

| 이벤트 | 시점 |
|---|---|
| `proposal_view` | 신청 화면 진입 |
| `proposal_industry_select` · `proposal_problem_select` | 산업·문제 선택 (`proposal_industry` 값 포함) |
| `proposal_form_start` | 폼 입력 시작 → **이탈자 리타게팅** 대상 |
| `proposal_submit` (`lead_type=custom_proposal`, `proposal_grade`) | 접수 성공 → **전환** (GA4 `generate_lead`·`lead_contact` 는 사이트 공통 `MonnitLead` 가 한 번만 보냄) |
| `proposal_status_view` · `proposal_visit_click` · `proposal_call_click` · `proposal_share` | 현황 화면 행동 |

Meta 픽셀이 있으면 접수 성공 시 `Lead` 이벤트도 함께 보냅니다(동의 모드 준수).
사이트 공통 리드 원장에도 `접점=custom_proposal`로 기록되어 기존 관제·먼데이에서 채널별로 보입니다.

**퍼널 장치**
- 신청 전: 산업 대표 사례·실시간 일치도로 「내 현장 이야기」라는 느낌을 먼저 줌
- 신청 후: 진행 화면 하단에 현장 진단 예약(`/visit`) 버튼 고정
- 발송 후: 열람 즉시 담당자 알림, 미열람 3일 뒤 후속 메일 1회
- 현황 화면의 「신청 페이지 공유」 버튼 → `utm_source=share` 로 추천 유입 측정

**시연용 화면** (영업 미팅·광고 소재 캡처용, 서버 기록 없음)
- `/proposal?demo=1` → 입력 후 시연 현황 화면으로 이동
- `/proposal/status?demo=1&co=회사명&nm=성함&ind=cold_chain`

---

## 9. 배포 전 확인

- [ ] v2 파일을 올렸었다면 `sh 맞춤제안서-이전파일-삭제.sh`
- [ ] **(중요) 아래 10장의 빠진 파일 확인**
- [ ] 배포 뒤 `/ops/proposals` → **「연동 점검」 → 「외부 서비스까지 확인」** 이 모두 정상인지
- [ ] `https://monnit.co.kr/assets/brand/monnit-korea-white-480.png` 가 열리는지 (메일 로고)
- [ ] 받은 PDF 1쪽에 한옥 배너, 2쪽부터 오른쪽 위 블루 로고
- [ ] 사이트를 EN으로 바꿔 `/proposal` · 진행 화면 · `/contact` 에 한글이 섞이지 않는지 (성함·회사명 제외)
- [ ] 진행 화면 사례 → 사례 화면 아래 「내 맞춤 제안서 진행 현황」 → 원래 화면으로
- [ ] `ops.mjs` 가 `/ops/*` 전체를 받는 설정이면 `/ops/proposals*` 가 가려질 수 있습니다 — 받은 사본에 `ops.mjs` 가 없어 확인하지 못했으니 배포 뒤 `/ops/proposals` 가 열리는지 봐 주세요
- [ ] 환경변수 `BREVO_API_KEY` · `PROPOSAL_SECRET` · `PROPOSAL_ADMIN_KEY` · `PROPOSAL_STAFF_TO` (방식은 기본 즉시)
- [ ] Brevo 발신 도메인(`monnit.co.kr`) 인증 · 하루 발송 한도
- [ ] 배포 로그에 `[proposal-sync]` 줄, Functions 에 `proposal-dispatch` 가 **Scheduled**
- [ ] 홈 파인더 → 「이 조건으로 맞춤 제안서 받기」 → 본인 이메일로 신청 → **3분 안에 메일 도착** · 진행 화면이 「발송 완료」로
- [ ] 주소창에 `?t=` 토큰이 남지 않는지, 뒤로가기로 파인더에 돌아오는지
- [ ] `/contact` 에서 「맞춤 제안서도 받기」 → 상담 접수 메일 + 제안서 진행 화면
- [ ] 휴대폰에서 `/proposal` 하단 버튼이 쿠키 바에 가리지 않는지
- [ ] `/ops/proposals` → 방금 건 등급 근거 · **확인 연락 완료** → 진행 화면에 반영
- [ ] 받은 메일의 PDF 링크 클릭 → 「제안서 열람」 알림
- [ ] 같은 이메일로 다른 과제를 다시 신청 → 「이미 받으신 제안서」 안내 + 담당자 「추가 제안 요청」 메일
- [ ] 진행 화면의 「현장 전체 견적 요청」 → 상담 폼에 회사·성함·견적 문구가 채워지는지
- [ ] `/ops` 로그인 뒤 탭 「맞춤 제안서」가 다시 로그인 없이 열리는지 (안 되면 `_ops_auth.mjs` 경로 확인 — 관리 키로는 계속 들어갈 수 있음)
- [ ] 상세 → 「Claude로 재생성」 → 검토 결과 → 「채택」 / 「버리기」 한 번씩
- [ ] 발송 대장 탭에 방금 건이 보이고 「PDF」가 열리는지 · 먼데이를 켰다면 보드에 항목이 생기는지
- [ ] 테스트 건은 관리 화면에서 **취소**

로컬 확인: `npm install` → `node tools/test-proposal.mjs` (155개 통과) → `node tools/check-proposal.mjs` → `node build.js` → `node tools/proposal-dev.mjs` 후 `http://localhost:8788/proposal`
(빠른 확인: `PROPOSAL_INSTANT_SECONDS=25 node tools/proposal-dev.mjs`)

---

## 10. ⚠️ 받은 저장소 사본에서 발견한 문제 (이번 작업과 별개)

받은 zip에는 다른 함수들이 불러오는 파일 여러 개가 **없습니다.**

```
netlify/functions/_store.mjs       ← lead · blockadmin · blockapi · _guard · sendpw 가 사용
netlify/functions/_ops_auth.mjs    ← blockadmin 이 사용
netlify/functions/_docmap.js       ← sendpw · getdoc · sendguide 가 사용
netlify/functions/_monday.mjs      ← lead 가 사용
netlify/functions/_alarmmail.mjs   ← lead 가 사용
netlify/functions/ops.mjs · _deals.mjs · health.mjs · visitadmin.mjs · mailpreview.mjs   ← 테스트·설정에서 참조
```

Netlify와 같은 방식으로 함수를 묶어 보면 `Could not resolve "./_store.mjs"` 로 **lead·sendpw·getdoc 등 기존 함수 빌드가 실패**합니다.
GitHub 저장소에는 있는데 zip만 빠진 것인지, 실제로 저장소에서 지워진 것인지 확인이 필요합니다.
실제로 빠졌다면 리드 접수(`/api/lead`)·제안서 다운로드·관제 화면이 배포 때 함께 깨집니다.

**이번 맞춤 제안서 기능은 이 파일들이 없어도 빌드·동작하도록** 경쟁사 차단 모듈을 느슨하게 연결해 두었습니다
(있으면 차단 규칙을 그대로 쓰고, 없으면 기본 규칙만 적용).

---

## 커밋 메시지

```
feat(proposal): 맞춤 제안서 v6 — 컨설팅 신청→현장 진단 예약 연결 · 백서 다운로드에 「우리 현장 맞춤 제안서도 받기」(modbus·proposal·temperature·whitepaper) · 입구 whitepaper

(v5) 맞춤 제안서 v5 — PDF 첫 장 한옥 배너 고정·브랜드 로고 · 「몇 시간 이내·접수 순서대로」 표현 · 글로벌 레퍼런스 문구 · 전체 영문 · 사례 이동 후 돌아가기 버튼 · 보안 강화(중복 응답 토큰 제거·출처·CSP·로그인 잠금) · 연동 점검 · 반응형 인터랙션

(v4) 맞춤 제안서 v4 — 과제 1개 무료 제안서·전체 견적 요청 연결 · 관제 탭/로그인 공유 · 발송 대장(PDF 사본·먼데이) · 고객 인사이트 · Claude 재생성→검토→결정
```
