# 설치 사진 페이지 CTA 추가 + 모바일/PC 최적화 — 적용 안내 (2026-09-16)

**바뀐 파일:** `installation-photos.html` 하나입니다. 전체 ZIP을 그대로 올리거나 이 파일만 덮어쓰고 커밋하면 됩니다.

**빌드 확인:** `node build.js`를 돌려 봤고 정상 완료됐습니다.
- 정적 페이지 117개 생성, 라우터 정합성 검사 통과
- 빌드가 만드는 폴더(`app/`, `contact/`, `promotions/` 등)는 기존처럼 Netlify가 배포할 때 생성하므로 ZIP에는 넣지 않았습니다.

## 1. 추가된 진입점
| 위치 | 내용 | 이동 주소 |
|---|---|---|
| 상단 바 | `프로모션` 링크 + `상담 문의` 버튼 (모바일은 상담 문의만) | `/promotions`, `/contact` |
| 제목 아래 | `우리 현장 상담받기` · `진행 중인 프로모션` 버튼 | 〃 |
| 사례 목록 6번째 뒤 | 상담 안내 배너. 업종 필터를 고르면 "{업종} 현장을 운영하고 계신가요?"로 문구가 바뀜 | `/contact`, 전화 |
| 사례 목록 24번째 뒤 | "마음에 드는 사례를 찾으셨나요?" 상담 배너 | 〃 |
| 사례 목록 48번째 뒤 | 프로모션 안내 배너 | `/promotions` |
| 검색 결과가 없을 때 | `사례 문의하기` · `프로모션 보기` 버튼 | 〃 |
| 사진 크게 보기 화면 | `이 현장과 비슷한 구성 문의` 버튼 (보던 사례 이름을 함께 넘김) | `/contact?...&case=사례명` |
| 페이지 하단 | 상담 · 프로모션 카드 2개 + 전화번호 · 이메일 | 〃 |
| PC, 스크롤 중 | 오른쪽 아래 `상담 문의` 버튼 (하단 카드가 보이면 숨김) | `/contact` |
| 모바일, 스크롤 중 | 화면 하단 고정 바: `전화` · `프로모션` · `상담 문의` | `tel:`, `/promotions`, `/contact` |

## 2. 최적화
- **사진 용량:** 페이지를 끝까지 스크롤할 때 받는 이미지가 약 **190MB에서 3.6MB**로 줄었습니다(73개 사례 기준 실측).
  - 기존에는 카드에 원본 사진(장당 평균 2~3MB)을 그대로 불러왔습니다.
  - 이제 카드에는 시트 `thumbs` 칸의 썸네일을 씁니다. 칸이 비어 있으면 680px WebP로 자동 축소합니다.
- **크게 보기 화면:** 원본 대신 화면 크기에 맞춘 WebP(최대 2000px)를 불러옵니다. 실패하면 원본을 불러옵니다.
- **좁은 폰(400px 이하):** 버튼이 한 줄에 끼이지 않도록 세로로 배치합니다.
- **상세 버튼:** 모바일 터치 영역을 36px 이상으로 키웠습니다.
- **점검 범위:** 320 · 360 · 390 · 414 · 768 · 1024 · 1440 · 1920px에서 확인했습니다. 가로 스크롤과 스크립트 오류는 없었습니다.

## 3. 유입 구분 · 측정
- **문의 링크 표시:** 문의 링크에 `?from=field-review&pos=위치`가 붙습니다. 문의 접수의 **유입 페이지** 칸에서 어느 버튼으로 왔는지 확인할 수 있습니다.
- **UTM 미사용:** 첫 광고 유입 출처 기록을 덮어쓰지 않도록 UTM 파라미터는 쓰지 않았습니다.
- **클릭 이벤트:** 클릭할 때마다 `dataLayer`에 `cta_click` 이벤트(`cta_position`, `cta_url`)가 기록됩니다.
- **주의:** 이 페이지에는 GTM(`monnit-consent.js`)이 아직 없어서 지금은 이 이벤트가 수집되지 않습니다.

## 4. 시트(InstallPhotos) 데이터 점검 결과 — 확인 필요
- **사진 주소 뒤 불필요한 문구:** 사진 주소 약 100곳 뒤에 ` :: C:\fakepath\01.jpg` 같은 업로드 흔적이 붙어 있습니다.
  - 페이지에서 자동으로 떼어내도록 처리했습니다.
  - 시트에서도 지워 두는 것이 좋습니다.
- **없어진 사진:** `코오롱이데아폴리스`의 사진 1장(`https://i.ibb.co/hFWJ4tBg/8ec5acc83ed9.jpg`)은 이미지 호스팅(ibb)에서 404가 납니다. 이 사진은 다시 올려야 합니다.
- **공개되지 않은 Dropbox 링크:** `한화 갤러리아백화점`, `한화 / 네이버`, `한화 이글스 (서산)`의 Dropbox 사진 3장은 링크 공개 범위가 '없음(no_one)'입니다.
  - 방문자에게는 사진이 보이지 않을 수 있습니다.
  - 공개 링크로 바꾸거나 ibb로 옮기는 것을 권합니다.
