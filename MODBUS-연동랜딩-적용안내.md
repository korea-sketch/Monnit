# Modbus 시스템 연동 기술지원 — 랜딩 발행 + 프로모션 등재

`/promo/modbus` 를 **광고 직링크 전용에서 사이트 정식 페이지로** 올립니다.
`/promotions` 목록에 「시스템 연동 기술지원」 카드가 생기고, 검색엔진에 색인됩니다.

새 서비스도, 새 환경변수도, 새 함수도 없습니다.
사이트에 이미 있는 규격(`monnit-consent` · `valid` · `monnit-lead` · `sendpw` · `getdoc` · `/api/lead`)만 씁니다.

---

## 1. 무엇이 바뀌나

**전** — `/promo/modbus` 는 2026-09-09 에 올린 광고 직링크 전용 페이지였습니다.
`noindex` 가 걸려 있어 검색에도, 사이트 안 어디에도 들어오는 길이 없었습니다.
광고를 끄면 그 순간 트래픽이 0이 되는 구조입니다.

**후** — 자료 자체를 「연동 매뉴얼」이 아니라 **「우리 환경에 맞는 구성을 제안하는 문서」** 로
다시 썼고, 페이지도 그 순서로 재편했습니다. `/promotions` 목록에 상시 카드로 올려
광고와 무관하게 유입이 남게 합니다.

카드는 할인 프로모션이 아니라 **기술지원 제안**으로 세웠습니다.
다른 카드가 「무료 체험」·「설치비 무료」로 말할 때, 이 카드는
「현장 구성을 같이 검토해 드립니다」로 말합니다. 찾아와야 할 사람이
설비·관제 담당자이기 때문입니다.

---

## 2. 자료의 관점이 바뀌었습니다

백서 43페이지의 뼈대가 이렇게 달라졌습니다.

| | 전 | 후 |
|---|---|---|
| 1장 | 게이트웨이 Modbus 설정법 | **AI·자동화보다 먼저 놓이는 것** — 계측이 없으면 데이터가 없다 |
| 2장 | 레지스터 주소 계산 | 무선을 관제에 붙일 때 실제로 걸리는 것 (끊김·보존·자기진단) |
| 3장 | 폴링 예제 | 왜 지금 가능한가 — FHSS · Encrypt-RF · 10년 배터리 |
| 4~8장 | (없음) | **산업별 구성 제안 4종** — 센서 수량과 점유 레지스터 주소까지 |
| 9~10장 | 트러블슈팅 | 게이트웨이 선택 기준 · 도입 전 체크리스트 |

「이렇게 설정하세요」에서 「이런 구성은 어떻습니까」로 바뀐 것이 핵심입니다.
설정법은 부록으로 내렸습니다.

---

## 2-1. 「수록 지면」 — 첫 화면에서 안을 먼저 보여 줍니다

히어로 바로 다음에 백서 지면 **세 장**을 놓았습니다. 눌러서 원본 크기로 볼 수 있습니다.
`/promo/temperature` 의 「이런 페이지가 들어 있습니다」와 같은 구조입니다.

무엇이 들어 있는지 모르는 PDF 를 받겠다고 회사 이메일을 적는 사람은 없습니다.
세 장은 「목차에 있는 것」이 아니라 **검토자가 실제로 확인해야 하는 것**으로 골랐습니다.

| 카드 | 백서 | 무엇을 보여 주나 |
|---|---|---|
| 현장별 구성 산출 | 11p · 구성안 ① 공장·제조 | 센서 종류 · 수량 · 점유 레지스터 주소 |
| Modbus 실측 검증 | 38p · 부록 D 연동 검증 | 센서 4대가 슬롯에 올라온 실제 화면 |
| 이상 시 진단 절차 | 39p · 부록 E 연동이 안 될 때 | 증상별 점검 순서 · 1차 점검 5가지 |

처음에는 레지스터 주소표(22p)와 도입 전 체크리스트(20p)를 넣었다가 뺐습니다.
둘 다 정확하지만 **받고 나서 참고할 자료**이지, 받을 이유가 되는 지면은 아닙니다.
주소표는 `40101 + 16 × (N − 1)` 한 줄이면 끝이고, 체크리스트는 우리가 요구하는
항목이지 검토자가 궁금해하는 것이 아닙니다.

**39p 를 일부러 앞에 세웠습니다.** 안 되는 경우를 먼저 인정하고 대응표를 보여 주는
쪽이 「기술지원」을 내건 프로모션과 앞뒤가 맞습니다.

이미지는 페이지에 인라인하지 않고 `/images/modbus/` 에 파일로 둡니다.
여섯 장을 base64 로 넣으면 페이지가 1MB 를 넘어 첫 화면이 그만큼 늦어집니다.
카드에는 560px 판을 쓰고, 「원본 크기로 보기」를 눌렀을 때만 1100px 판을 받아 옵니다.

**백서를 고치면 이 세 장도 다시 뽑아야 합니다.** 페이지 번호가 밀리면 엉뚱한
장이 걸립니다.

```
pdftoppm -jpeg -r 150 -f 11 -l 11 <백서.pdf> config     # 11p → config
#                      -f 38 -l 38            proof     # 38p → proof
#                      -f 39 -l 39            trouble   # 39p → trouble
# 각각 1100px · 560px 두 벌로 줄여 images/modbus/ 에 둡니다
```

---

## 3. 흐름

```
/promotions 카드 · 검색 · 광고
            ↓
      /promo/modbus
            ↓
   안을 먼저 보기 → 왜 계측인가 → 왜 Monnit인가 → 구성안 4종 → 붙이는 절차
            ↓
        [백서 PDF 받기]
            ↓
  폼 (회사명 · 이름 · 회사이메일 필수 3개)
            ↓
  ① sendpw → 10분 서명 링크 → getdoc → PDF 전달
  ② MonnitLead.track('doc_request') → /api/lead → 관제(ops) 화면
            ↓
      현장 구성 검토 신청 CTA
```

PDF 는 공개 주소가 없습니다. `/proposals/*` 는 `_redirects` 에서 404 로 막혀 있고,
`netlify.toml` 의 `included_files = ["proposals/**"]` 가 이미 함수에 물려 있어
새 PDF도 그대로 보호됩니다. **두 파일 모두 손댈 것이 없습니다.**

---

## 4. 바뀐 · 추가된 파일

### 발행 대상

| 파일 | 내용 |
|---|---|
| `promo-modbus.html` | **신규** — 랜딩 원본 (318 KB, 이미지 인라인) |
| `promo/modbus/index.html` | 위 파일의 사본 — `build.js` 가 자동 생성 |
| `promo/modbus/og-image.jpg` | 교체 — 링크 미리보기 1200×630 |
| `proposals/modbus-integration-whitepaper.pdf` | 교체 — 43페이지 구성 제안판 (3.1 MB) |
| `images/promo-modbus.webp` | **신규** — 프로모션 카드 썸네일 (34 KB) |
| `images/promo-modbus.jpg` | **신규** — 위 이미지의 예비본 (95 KB) |
| `images/modbus/*.jpg` | **신규** — 백서 페이지 미리보기 6장 (560px 3장 · 1100px 3장, 합 450 KB) |

### 배선

| 파일 | 내용 |
|---|---|
| `app.js` | `BUILTIN_PROMOS` 에 `modbus` 추가 · 전체 order 1~5 재배치 · 시트 order 파싱 버그 수정 |
| `editor.html` | 내장 프로모션 목록을 `app.js` 와 동기화 · 순서 패널에 썸네일 |
| `build.js` | `/promo/modbus` 별칭 생성기 추가 · sitemap 에 한 줄 · `source/` 주석 보존 |
| `_redirects` | `/promo/modbus` 200 rewrite + 구 주소 301 3종 |

### 원본 보관

`source/modbus-whitepaper/` 아래에 랜딩 템플릿 · 테마 · 도식 생성기 · 백서 원본을
그대로 두었습니다. 다음에 고칠 때 이 폴더에서 고치고 `landing/build.py` 를 돌리면
`promo-modbus.html` 이 다시 나옵니다.

`netlify/functions/_docmap.js` 는 **손댈 것이 없습니다.** 2026-09-09 에 넣은
줄이 그대로 맞습니다:

```js
'무선센서 Modbus 연동 백서': ['modbus-integration-whitepaper.pdf',
                              '무선센서_Modbus_연동_백서_MonnitKorea.pdf'],
```

랜딩의 `DOC_TITLE` 이 이 키와 **글자 하나까지 같아야** 합니다. 다르면 `not_ready` 로 떨어집니다.
`landing/build.py` 가 빌드할 때마다 이 값을 찍어 주니 눈으로 확인하십시오.

---

## 5. 프로모션 카드

`app.js` 의 `BUILTIN_PROMOS` 에 들어간 내용입니다. **구글 시트에 행을 넣지 않아도
목록에 나옵니다.** 시트에 같은 `id`(`modbus`) 행을 만들면 시트 값이 우선합니다.

| 항목 | 값 |
|---|---|
| id | `modbus` |
| 제목 | PLC·BMS·SCADA 시스템 연동 기술지원 |
| 배지 | 기술지원 |
| 기간 자리 | 무료 현장 구성 검토 · 연동 자료 제공 |
| 링크 | `/promo/modbus` |
| 시작 | 2026-09-15 (한국시간 00:00) |
| 종료 | 없음 (상시) |
| 순서 | 3 (알리미 1 · 예지보전 2 · modbus 3 · 레지던스 4) |

**`link` 가 `/promo/` 로 시작한다는 점이 중요합니다.** `build.js` 는 그런 항목에 대해
`/promotions/modbus` 상세 페이지를 따로 만들지 않습니다. 만들면 실제 랜딩과
주소가 둘로 갈려 검색엔진이 어느 쪽을 정본으로 볼지 헷갈립니다.
`PROMO_SEO` 에도 넣지 마십시오 — 같은 이유로 `/promotions/modbus` 가 생깁니다.

내릴 때는 `app.js` 의 해당 객체에서 `end: '2026-XX-XX'` 를 채우거나 블록째 지우면 됩니다.

---

## 6. 검수에서 고친 것

배포 전에 잡아 둔 것들입니다. 같은 실수가 반복되기 쉬운 지점이라 적어 둡니다.

**공통 스크립트가 두 번 로드되고 있었습니다.**
`monnit-consent.js` · `valid.js` · `monnit-lead.js` 가 `<head>` 와 푸터에 각각
한 벌씩 들어 있었습니다. 그대로 뒀다면 동의 배너가 두 번 뜨고 GTM 이 중복
주입됩니다. 푸터 쪽을 걷어냈습니다.

**GA4 와 Meta 픽셀을 직접 한 번 더 부르고 있었습니다.**
`monnit-consent` 가 이미 GTM(`GTM-T8H73VW`)을 올리고, GA4 와 픽셀은 그 안에서
발화합니다. 페이지에 `gtag('config', …)` 와 `fbq('init', …)` 이 또 있으면
**같은 페이지뷰가 두 번 잡혀 광고 성과가 2배로 부풀어 보이고**, 픽셀은
동의 배너보다 먼저 떠서 동의 관리를 우회합니다. 두 블록 모두 제거했습니다.

**Pretendard 웹폰트를 jsDelivr 에서 받아 오고 있었습니다.**
사이트 나머지 130페이지는 웹폰트를 받지 않고 시스템 글꼴 스택만 씁니다.
이 페이지만 다른 글꼴로 보이고 외부 호스트에 렌더링이 묶이므로 걷어내고,
`index.html` 과 한 글자도 다르지 않은 스택으로 맞췄습니다.
(사이트 전체에 Pretendard 를 넣기로 한다면 그때 함께 하는 것이 맞습니다.)

**구성안 카드의 줄이 맞지 않았습니다.**
카드마다 센서 종류 수가 달라 표 높이가 어긋났고, 그 아래 합계 블록과 설명
글머리표가 카드별로 다른 높이에서 시작했습니다. CSS `subgrid` 로 두 카드가
같은 행 격자를 공유하게 바꿔, 항목 수가 달라도 합계·설명 줄이 나란히 옵니다.
(`subgrid` 를 모르는 구형 브라우저는 `@supports` 밖에서 기존대로 보입니다.)

**전화번호가 어디에도 없었습니다.**
자료를 받아 가기보다 한 번 통화하는 편이 빠른 분이 늘 있습니다.
상단 바 · 히어로 · 기술지원 세 곳에 `02-2088-1454` 를 넣고 `tel:` 로 걸어
휴대폰에서 눌러 바로 걸리게 했습니다. 검색 결과에도 번호가 뜨도록
`Organization` JSON-LD 에 전화 · 주소 · `contactPoint` 를 넣었습니다.
주소가 **8층**으로 되어 있던 것을 **2층**으로 바로잡고, 백서 표지와 부록
연락처에도 같이 반영해 PDF 를 다시 만들었습니다.

번호나 주소가 바뀌면 **화면 세 곳과 JSON-LD, 그리고 백서 두 곳**을
같이 고쳐야 합니다.

**카드 순서가 화면과 정적 스냅샷에서 서로 달랐습니다.**
`parseInt(order,10) || 999` 는 `order` 가 **0일 때 0이 falsy** 라 999로 바뀝니다.
맨 앞에 두려던 카드가 맨 뒤로 갑니다. 화면(`app.js` `sortPromos`)은 `(a.order||0)`
을 써서 0을 살리는데 정적 스냅샷(`build.js`)과 시트 파싱은 `||999` 를 써서,
같은 목록이 두 곳에서 다른 순서로 나오고 있었습니다. 양쪽 다 `Number.isFinite`
로 고치고, 에디터의 「순서 저장」이 1,2,3… 을 쓰므로 내장 `order` 도 1부터로
맞췄습니다.

**에디터에서 순서를 못 바꾸고 있었습니다.**
드래그 기능은 처음부터 있었습니다 — `↕ 순서 바꾸기(드래그)·삭제` 버튼을 누르면
목록을 끌어서 옮기고 「순서 저장」을 누르면 됩니다. 안 보였던 이유가 둘입니다.
하나, `editor.html` 의 `BUILTIN_PROMO_ROWS` 가 `app.js` 와 어긋나 **알리미와
modbus 가 목록에 아예 없었습니다.** 없는 것은 끌 수 없습니다. 다섯 건 모두
맞췄습니다. 둘, 목록이 글자만이라 어느 카드인지 알아볼 수 없었습니다.
썸네일을 붙이고 버튼 이름도 고쳤습니다.

**`build.js` 가 `source/` 안의 주석까지 지우고 있었습니다.**
`source/` 는 서빙되지 않는 작업 원본이고, 주석이 그 파일의 내용입니다.
주석 제거 대상에서 제외했습니다.

---

## 7. 웹·모바일 확인 결과

| 항목 | 결과 |
|---|---|
| `<meta viewport>` · doctype · charset · `lang` | 정상 |
| 중복 `id` | 없음 |
| 없는 자산 참조 | 없음 (사이트 270개 HTML 전수 검사) |
| 그리드 붕괴 지점 | 940 / 860 / 480px 세 곳에서 순차적으로 1열 전환 |
| 표 (`table.cfg` 4개) | `min-width:0` — 360px 에서도 가로 넘침 없음 |
| 도식 SVG | `.dgm` 에 `overflow-x:auto` — 좁은 화면에서 도식만 가로 스크롤 |
| 버튼 터치 영역 | 48px 이상 · 480px 이하에서 전폭 |
| 외부 호스트 | `fonts.googleapis.com` 하나 (사이트 나머지와 동일) |
| 페이지 용량 | 318 KB (이미지 인라인 포함) |
| `prefers-reduced-motion` | 대응함 — 모션 끔 |

사이트 전체 검사에서 이 작업과 **무관하게** 남아 있는 건 아래 두 가지입니다.
이번 커밋에서는 건드리지 않았습니다.

- `installation-photos.html` · `pdm-guide 2/index.html` — 스크립트가 만드는 `<img>` 에 `alt` 없음
- `promo-alarm.html` — 편집기 샘플 코드 안의 `<img src="이미지주소">` (실제 렌더 대상 아님)

---

## 8. 올린 뒤 확인할 것

1. `https://monnit.co.kr/promotions` 에 「PLC·BMS·SCADA 시스템 연동 기술지원」 카드가 보이는가
2. 카드를 누르면 `https://monnit.co.kr/promo/modbus` 로 가는가 (`/promotions/modbus` 가 아님)
3. `https://monnit.co.kr/promo-modbus.html` 이 `/promo/modbus` 로 301 되는가
4. 폼에 회사 이메일을 넣으면 **메일로 10분짜리 링크**가 오는가
5. 그 링크로 받은 PDF 가 **43페이지** 판인가
6. 관제(ops) 화면에 접수가 `promo_modbus` 로 뜨는가
7. GA4 실시간에서 이 페이지 페이지뷰가 **1회만** 잡히는가 (중복 제거 확인)
8. `https://monnit.co.kr/sitemap.xml` 에 `/promo/modbus` 가 있는가

---

## 9. 아직 확인이 필요한 것

자료 안에 들어간 숫자 중 제가 확정할 수 없는 것들입니다.
`source/modbus-whitepaper/확인필요-백서보완.md` 에 같은 내용이 정리돼 있습니다.

- **센서 종류 80종+** — 백서 옛 판은 55종으로 적혀 있었고 monnit.co.kr 은 80종입니다. 현재는 80으로 통일했습니다.
- **비용 관련 수치** — 「15분 설치」·「1/30」·「−90%」·「6~12개월 회수」가 지금도 유효한지
- **KC 인증 · 국내 주파수** 표기
- **영하 온도(음수) 레지스터 처리** · **진동 센서 채널 표** · **게이트웨이 자체 레지스터 표** · **폴링 주기 설계 기준** — 부록에 넣을 실측 자료
- **ALTA Ethernet Gateway 실제 제품 컷아웃 사진** — 현재 페이지에는 넣지 않았습니다

---

*작성 2026-09-15 · 모넷코리아*
